# 온프레미스 vs AWS EKS 비교 실험 — 프로젝트 결과 보고서 (상세)

> 동일한 쇼핑몰 애플리케이션을 온프레미스 Kubernetes와 AWS EKS에 똑같이 배포하고,
> 트래픽 폭증·노드 장애 상황에서 두 환경이 어떻게 다르게 버티는지를 데이터로 비교하는 실험 프로젝트.

*작성자: 팀원C(본인) · 기간: 2026.05 ~ 진행 중(약 4주+) · 본 보고서의 개인 담당 영역: 앱 개발 · 온프레미스 인프라 구축 · 서비스 배포*

> ※ 기간·세부 수치는 추후 본실험 완료 시 갱신 예정. 현재는 인프라·배포 완료, 본실험 진행 단계.

---

## 1. 프로젝트 개요

### 1.1 배경·목적

이커머스 타임세일처럼 트래픽이 순간적으로 폭증하는 상황에서, **자체 보유 인프라(온프레미스)** 와 **클라우드 관리형 인프라(AWS EKS)** 가 실제로 어떤 차이를 보이는지 정량적으로 확인하기 위한 실험.

핵심 설계 원칙은 **"동일한 앱, 동일한 트래픽, 단 하나의 변수만 다르게"**.
앱 코드·부하 패턴·Kubernetes 설정을 전부 동일하게 맞추고, **노드 자동 확장(오토스케일링) 유무**만 독립변수로 둔다.

| 항목 | 온프레미스 | EKS |
|---|---|---|
| 파드 자동확장 (HPA) | 동일 | 동일 |
| **노드 자동확장 ★핵심 변수** | 없음 (고정 자원) | Karpenter (탄력 자원) |
| DB 자동복구 (선택) | 수동 | RDS Multi-AZ |

> "EKS가 더 좋다"를 강요하는 실험이 아니라, **고정 자원 환경과 탄력 자원 환경의 운영 특성 차이를 숫자로 드러내는 것**이 목적. 온프레미스에서 파드 Pending이 쌓이는 것은 실패가 아니라 측정 대상.

### 1.2 시스템 구성

| 구분 | 내용 |
|---|---|
| 애플리케이션 | MSA 7개 서비스 (gateway + product/inventory/order/payment/user/frontend) |
| 온프레미스 | AWS EC2(c8i.2xlarge) 위에 KVM 가상머신으로 k8s 클러스터 재현 (master + worker 3대) |
| 이미지 레지스트리 | GHCR (`ghcr.io/incheon-soda/shoply-*`) |
| 데이터베이스 | 온프레: EC2 PostgreSQL 16 / EKS: RDS |
| 부하 도구 | k6 |
| 모니터링 | Prometheus + Grafana (별도 EC2) |
| Kubernetes | v1.34.x, CNI Flannel v0.26.7 |

---

## 2. 팀 차원 (맥락)

### 2.1 팀 구성

총 **5명, 2개 팀**으로 구성. 동일한 앱·동일 기준으로 두 환경을 각각 구축·실험.

| 팀 | 인원 | 담당 |
|---|---|---|
| **온프레미스 팀** | 팀원C(본인) | 앱 개발 · 온프레미스 인프라 구축 · 서비스 배포 |
| | 팀원D | 모니터링 (Prometheus / Grafana) |
| | 팀원E | 부하 테스트 · 부하 시나리오 · CI/CD (온프레·EKS 공통) |
| **EKS 팀** | 팀원A, 팀원B | EKS 환경 구축 (세부 분담 별도) |

### 2.2 전체 목표·최종 산출물

- **목표:** 두 환경에서 동일 실험(안정 → 스파이크 → 노드 장애)을 수행하고, 한계 RPS·복구 시간(MTTR)·에러율·매출 손실 환산 등을 비교한 결과 리포트 도출.
- **최종 산출물:** 온프레/EKS 비교 실험 결과 + 발표 자료.

### 2.3 진행 현황

| 영역 | 상태 |
|---|---|
| 온프레미스 인프라(KVM k8s) 구축 | 완료 |
| 앱 7개 서비스 개발 · 이미지 빌드(GHCR) | 완료 |
| 서비스 배포 · HPA 작동 확인 | 완료 |
| 모니터링(Grafana 대시보드) | 완료 |
| 본실험(한계 RPS·MTTR 정식 측정) | **진행 중** |
| EKS 환경 구축 및 양 환경 비교 | **진행 중** |

> 현재까지 두 환경 중 **온프레미스 쪽 인프라·앱·배포가 완료**되었고, 정식 비교 실험과 EKS 구축은 진행 중인 단계.

---

## 3. 개인 차원 (핵심)

> 담당 4영역: **앱 빌드 · 온프레미스 인프라 · 서비스 배포 · 실험**. 코드부터 클러스터, 배포, 실험 환경까지 온프레미스 전 과정을 담당.

### 3.1 역할과 책임 범위

- **주 담당:** 쇼핑몰 MSA 앱 개발, 온프레미스 k8s 인프라 구축, 서비스 배포.
- **보조 참여:** 모니터링·부하(k6)·CI/CD는 각 담당(팀원D·E) 영역이며, 작동 확인 수준에서 함께 살펴봄.
- **비담당:** EKS 클러스터 구축(EKS 팀). 단, EKS도 동일 앱 이미지·매니페스트를 사용하므로 **동일화 기준을 제공**.

### 3.2 실제로 한 작업

#### (1) 앱 개발 — MSA 7개 서비스

| 서비스 | 핵심 역할 |
|---|---|
| gateway | 모든 API 진입점. 각 마이크로서비스로 프록시 + Prometheus 메트릭 수집 |
| product | 상품 조회. Redis 캐시(목록 60초/상세 30초) |
| inventory | 재고 관리. **SELECT FOR UPDATE 동시성 제어**(reserve/deduct/release) |
| order | 주문 생성. 재고 예약 → 주문 저장(트랜잭션) |
| payment | 결제 처리(Mock 95% 성공). 성공 시 재고 차감, 실패 시 예약 해제 |
| user | 로그인/인증(bcrypt + JWT) |
| frontend | React 쇼핑몰 UI |

- 백엔드 Express(TypeScript), 프론트 React로 7개 서비스 구현.
- 재고 동시성(`SELECT FOR UPDATE`)으로 동시 주문이 몰려도 **초과판매(oversell) 방지** 처리.
- 서비스별 Dockerfile 작성, `git push → GitHub Actions → GHCR 자동 푸시` 흐름으로 이미지 관리.
- `docker compose build` 전체 통과로 빌드 검증.

#### (2) 온프레미스 인프라 — EC2 위 KVM k8s 재현

- **목표:** 물리 온프레미스 환경(오토스케일링 없는 고정 자원)을 AWS EC2 1대 위에 KVM 가상머신으로 재현.
- EC2 구성: k8s 호스트(c8i.2xlarge, KVM VM) + PostgreSQL EC2 + Redis EC2.
- **중첩 가상화 이슈 해결:** c8i-flex 인스턴스에서 KVM 가상화가 비활성이라 처음엔 LXD 컨테이너로 구성 → 중첩 가상화를 활성화해야 함을 확인 → 스팟 요청 페이지엔 옵션이 없어 **launch template를 만들어 중첩 가상화를 켜서 KVM 작동**시킴.
- cloud-init으로 VM 초기화, containerd·kubeadm/kubelet/kubectl 직접 설치, `kubeadm init` + worker `join`, Flannel CNI, metrics-server, Nginx Ingress 구성.
- **워커 3대 분리 설계:** worker1·2 = 실험 전용 / worker3 = 운영 전용(taint 격리). Nginx Ingress·kube-state-metrics 등 운영 보조 파드를 worker3로 몰아 **실험 워커를 "순수 고정 자원"으로** 만들어 측정 신뢰도 확보.
- **네트워크:** KVM VM에 공인 IP가 없어 EC2 iptables DNAT로 외부 트래픽을 내부로 포워딩, MetalLB로 Ingress를 LoadBalancer 추상화(EKS NLB와 동일 추상화로 공정 비교).
- **백업·복원 체계:** 스팟 인스턴스 회수에 대비해 호스트 EC2를 AMI로 백업/복원, KVM 디스크(qcow2) 백업/복원 절차 마련.

#### (3) 서비스 배포 — k8s 매니페스트

- 서비스별 Deployment / Service(ClusterIP·NodePort) / HPA / ConfigMap / Secret / Ingress 구성.
- 매니페스트는 AI 도움을 받아 구조를 잡고 값을 조정. 배포 자체는 이미지 pull·CrashLoop·Pending 등 큰 이슈 없이 진행.
- **ConfigMap DB 연결 정상화:** 초기에는 DB 호스트 설정의 필요성을 몰라 누락 → 인프라를 여러 번 구축하며 ConfigMap에 PostgreSQL/Redis EC2 사설 IP를 넣어야 파드가 DB에 붙는다는 점을 파악하고 반영.
- 배포 검증: 로그인 토큰 발급(admin·test 계정), 주문→결제 흐름, 부하 시 파드 증가 확인.

#### (4) 실험 환경 — 시나리오 설계

| 시나리오 | 내용 | 관찰 대상 |
|---|---|---|
| 1 안정 | 200 RPS 정상 | 양쪽 정상(신뢰 형성) |
| 2 스파이크 ★ | 급증 후 유지 | 온프레 Pending 지속 vs EKS 회복 |
| 3 노드 장애 | worker 종료 | MTTR(수동 vs 자동) |
| (4 DB 장애, 선택) | Primary DB 종료 | DB 페일오버 |

- 실험이 유의미하려면 **유지 구간**이 필요하다는 점(짧은 스파이크는 반응 전에 끝남), 재고가 먼저 바닥나면 실험이 오염되므로 `load-test-prep.sql`로 상품 재고를 충분히 채워야 한다는 점 등 실험 환경 조건을 정립.

### 3.3 내 판단·기여

- **HPA 설정 조정 (파드 증가 확인 목적):** 초기에는 각 서비스 replicas 2개 + HPA max 제한 상태였는데, 부하를 줘도 파드가 잘 늘지 않았음. **파드가 실제로 늘어나는지 확인하기 위해** replicas 1개 시작 + max를 크게(99) 풀어, 부하를 주면 파드가 증가하는지를 눈으로 검증할 수 있게 바꿈.
- **운영/실험 노드 분리(taint 격리):** 운영 보조 파드가 실험 워커 자원을 갉아먹어 측정을 흐리는 문제를 인지하고 worker3로 격리 → 실험 워커를 순수 부하만 받는 환경으로 구성.
- **하이브리드 부하 설계:** 깨끗한 앱은 가벼워 HPA가 잘 안 도는 문제를, request를 작게(100m) 두고 RPS를 크게 주는 방식으로 보완.
- **재현 가능성 확보:** AI 가이드를 따라가다 설치 항목을 반복적으로 누락하는 시행착오를 겪고, **설치 절차를 직접 문서로 고정**해 재현 가능하게 만듦.

### 3.4 내 작업의 결과 (현재까지 검증된 것)

- 온프레미스 KVM k8s 클러스터(master + worker 3대) **정상 가동**.
- 앱 7개 서비스 배포 후 로그인·주문·결제 흐름 **정상 동작 확인**.
- 부하를 주면 HPA가 작동해 파드가 증가하는 것을 확인. 최대 약 60개까지 증가했으나, 이 구간은 **Pending·스케줄 실패가 함께 발생한 상태**로, 정식 한계치 측정이 아니라 **"부하가 실제로 들어가고 파드가 늘어나는지"를 확인하는 수준**이었음.
- 워커 노드 CPU가 최대 80~90%까지 올라가는 것을 Grafana에서 실시간 관찰.

> **아직 측정되지 않은 것:** 정식 한계 RPS, 노드 장애 MTTR, 스파이크 에러율, 결제 실패→매출 손실 환산. 이 수치들은 본실험(진행 중)에서 채울 예정이며, 현재 보고서에는 검증된 사실만 기재함.

---

## 4. 회고 · 배운 점

- **"다 한 것 같은데" 함정:** AI 가이드를 따라가면 끝난 것처럼 보여도 설치 항목을 하나씩 빠뜨려 재설치를 반복하게 됨. 결국 **직접 절차를 문서로 고정**하는 것이 재현성과 시간 절약의 핵심임을 체감.
- **측정 환경은 설계의 일부:** 운영 파드가 섞이면 "고정 자원의 순수 한계"를 측정할 수 없음. **무엇을 측정하려는지에 맞춰 노드 배치까지 설계**해야 한다는 점을 배움(worker3 격리).
- **실험은 조건이 90%:** 부하의 세기뿐 아니라 유지 시간, 재고 사전 세팅 등 조건을 맞추지 않으면 결과가 오염됨.
- **인프라 문제 해결 경험:** 중첩 가상화, iptables DNAT, ConfigMap DB 연결 등 "왜 안 되지?"에서 원인을 찾아 해결하는 과정에서 온프레미스 인프라의 동작 원리를 깊게 이해.
- **협업·역할 경계:** 5명/2팀 구조에서 동일화 기준(앱 이미지·매니페스트)을 EKS 팀에 제공하는 등, 내 산출물이 다른 팀의 입력이 되는 협업 구조를 경험.

### 앞으로 할 것

- 부하를 더 키워 **정식 한계 RPS** 측정.
- 시나리오 1·2·3 본실험 수행 → **MTTR·에러율** 측정.
- EKS 환경 구축 후 동일 기준으로 비교 → 결과 리포트 완성.

---

*본 보고서는 현재까지 자료·실측에 근거하며, 미완 항목은 "진행 중"으로 명시함. 과장 없이 검증된 사실 위주로 작성됨.*
