# 온프레미스 vs AWS EKS 비교 실험 — 프로젝트 결과 보고서 (요약)

*팀원C(본인) · 2026.05 ~ 진행 중 · 담당: 앱 개발 · 온프레미스 인프라 구축 · 서비스 배포*

## 개요

동일한 쇼핑몰 MSA 앱을 **온프레미스 k8s**와 **AWS EKS**에 똑같이 올리고, **노드 자동확장 유무**만 변수로 두어 트래픽 폭증·노드 장애 시 운영 특성 차이를 데이터로 비교하는 실험. (온프레: 고정 자원 / EKS: Karpenter 탄력 자원)

## 팀 구성 (5명 / 2팀)

- **온프레미스 팀:** 팀원C(본인, 앱·인프라·배포) / 팀원D(모니터링) / 팀원E(부하·시나리오·CI/CD)
- **EKS 팀:** 팀원A·B (EKS 구축)
- 최종 목표: 두 환경 동일 실험 후 한계 RPS·MTTR·에러율 비교 리포트. **현재 온프레 인프라·앱·배포 완료, 본실험·EKS 진행 중.**

## 내가 한 일 (핵심)

- **앱 개발:** Express(TS)+React로 MSA 7개 서비스 구현. 재고 동시성(`SELECT FOR UPDATE`)으로 초과판매 방지. GitHub Actions → GHCR 이미지 빌드.
- **온프레미스 인프라:** EC2 위 KVM으로 k8s(master+worker 3대) 재현. **중첩 가상화 이슈를 launch template로 해결**, kubeadm·Flannel·Ingress 직접 구성. **worker3 taint 격리**로 실험 워커를 순수 고정 자원화(측정 신뢰도 확보). iptables DNAT·MetalLB 네트워크, AMI/VM 백업·복원 체계 마련.
- **서비스 배포:** Deployment/HPA/ConfigMap/Secret/Ingress 매니페스트 구성·배포. ConfigMap에 DB·Redis 사설 IP를 넣어 DB 연결 정상화. 로그인·주문·결제 흐름 검증.
- **실험 환경 설계:** 안정→스파이크→노드장애 시나리오 설계, 유지 구간·재고 사전세팅 등 측정 조건 정립.

## 판단·기여

- **HPA replicas 1 + max 99로 조정** — 부하를 줬을 때 파드가 실제로 늘어나는지 확인하기 위함.
- **운영/실험 노드 분리** — 운영 파드가 실험 워커 자원을 갉아먹는 문제를 인지해 worker3로 격리.
- **재현 가능한 설치 절차 문서화** — AI 가이드의 설치 누락 반복을 직접 절차 고정으로 해결.

## 결과 (현재까지 검증된 것)

- 온프레미스 k8s 클러스터 정상 가동, 앱 7개 배포·로그인·주문·결제 정상 동작 확인.
- 부하 시 HPA 작동으로 파드 최대 약 60개 증가 확인. 단, 이 구간은 **Pending·실패가 함께 발생한 상태**로 정식 한계 측정이 아니라 **부하 인입·파드 증가 확인 수준**. 워커 CPU 80~90% 관찰.
- **미측정(본실험 예정):** 정식 한계 RPS, 노드 장애 MTTR, 스파이크 에러율, 결제 실패→매출 손실 환산.

## 배운 점

설치 절차를 직접 문서로 고정해야 재현성이 확보됨 · 측정 목적에 맞춰 노드 배치까지 설계해야 함(운영 파드 격리) · 중첩 가상화·iptables·ConfigMap 등 인프라 문제를 원인부터 찾아 해결하며 온프레미스 동작 원리를 이해.
