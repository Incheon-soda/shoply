# 02. 데이터베이스

> Shoply MSA — PostgreSQL 16 + Redis 7  
> 스키마 설계부터 온프레미스 Docker · EKS RDS 구축까지

---

## 목차

1. [DB 구성 개요](#1-db-구성-개요)
2. [스키마 — 테이블 상세](#2-스키마--테이블-상세)
3. [ERD](#3-erd)
4. [인덱스 전략](#4-인덱스-전략)
5. [서비스별 핵심 쿼리](#5-서비스별-핵심-쿼리)
6. [동시성 처리 — SELECT FOR UPDATE](#6-동시성-처리--select-for-update)
7. [Redis 캐싱 구조](#7-redis-캐싱-구조)
8. [온프레미스 구축 — EC2 + Docker](#8-온프레미스-구축--ec2--docker)
9. [EKS 구축 — RDS](#9-eks-구축--rds)
10. [시드 데이터](#10-시드-데이터)
11. [실험 전 초기화 체크리스트](#11-실험-전-초기화-체크리스트)
12. [로컬 개발 환경 검증 (Docker Compose)](#12-로컬-개발-환경-검증)
13. [관리 쿼리 모음](#13-관리-쿼리-모음)

---

## 1. DB 구성 개요

### 환경별 구성

| 항목 | 온프레미스 | EKS |
|------|-----------|-----|
| **PostgreSQL** | 별도 EC2 (t3.medium) + Docker | AWS RDS (db.t3.medium) |
| **Redis** | 별도 EC2 (t3.micro) + Docker | 별도 EC2 (t3.micro) + Docker |

> Redis는 양쪽 모두 EC2 Docker로 동일하게 구성 — **통제 변수 (비교 대상 아님)**  
> PostgreSQL만 온프레미스(직접 관리) vs EKS(RDS 관리형)으로 차이를 둔다

### PostgreSQL 역할

모든 서비스가 **하나의 PostgreSQL 인스턴스**를 공유하되, 테이블 소유권은 서비스별로 분리된다.

| 서비스 | 소유 테이블 | 접근 방식 |
|--------|------------|----------|
| User | `users` | SELECT (로그인 시 1회) |
| Product | `products` | SELECT 다수, UPDATE (타임세일) |
| Inventory | `inventory` | **SELECT FOR UPDATE · UPDATE** (동시성 핵심) |
| Order | `orders`, `order_items` | INSERT, SELECT |
| Payment | `payments` | INSERT, UPDATE |

### Redis 역할

Product Service 전용 캐시. **재고(inventory)는 절대 캐시하지 않는다.**

```
상품 목록 조회 → Redis HIT  → DB 쿼리 없이 즉시 반환
               → Redis MISS → DB SELECT → Redis 저장 → 반환

재고 조회      → 항상 PostgreSQL 직접 조회 (캐시 없음)
```

---

## 2. 스키마 — 테이블 상세

### 파일 위치

```
msa_shoply/db/schema.sql   ← DDL
msa_shoply/db/seed.sql     ← 초기 데이터 (500개 상품, 계정 11개+)
```

---

### users

> User Service 소유. 회원 정보 저장.

```sql
CREATE TABLE IF NOT EXISTS users (
  id         UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
  email      VARCHAR(255) UNIQUE NOT NULL,
  password   VARCHAR(255) NOT NULL,    -- bcrypt(cost=10) 해시값
  name       VARCHAR(255) NOT NULL,
  created_at TIMESTAMPTZ  NOT NULL DEFAULT NOW()
);
```

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | PK, `gen_random_uuid()` 자동 생성 |
| email | VARCHAR(255) | UNIQUE 제약 — 중복 가입 방지 |
| password | VARCHAR(255) | bcrypt 해시 (평문 저장 절대 불가) |
| name | VARCHAR(255) | 표시 이름 |
| created_at | TIMESTAMPTZ | 가입 시각 |

---

### products

> Product Service 소유. 상품 정보 + 타임세일 상태 저장.

```sql
CREATE TABLE IF NOT EXISTS products (
  id           UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
  name         VARCHAR(255) NOT NULL,
  price        INTEGER      NOT NULL,       -- 정상가 (원 단위)
  description  TEXT,
  image_url    VARCHAR(500),
  is_timesale  BOOLEAN      NOT NULL DEFAULT FALSE,
  sale_price   INTEGER,                     -- 세일가 (NULL = 세일 아님)
  sale_ends_at TIMESTAMPTZ,                 -- 세일 종료 시각 (NULL = 세일 아님)
  created_at   TIMESTAMPTZ  NOT NULL DEFAULT NOW()
);
```

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | PK |
| name | VARCHAR(255) | 상품명 (영문) |
| price | INTEGER | 정상가 (단위: 원) |
| description | TEXT | 상품 설명 |
| image_url | VARCHAR(500) | 이미지 URL |
| is_timesale | BOOLEAN | 타임세일 진행 여부 |
| sale_price | INTEGER | 세일 적용 가격. NULL이면 정상가로 노출 |
| sale_ends_at | TIMESTAMPTZ | 세일 종료 시각. 이 시각 이후엔 자동 만료 처리 |

**타임세일 로직:**
- `is_timesale = TRUE` + `sale_price` + `sale_ends_at` 세 가지가 동시에 설정됨
- 타임세일 종료 시 `is_timesale = FALSE`, `sale_price = NULL`, `sale_ends_at = NULL`으로 복구

---

### inventory

> Inventory Service 소유. **실험의 핵심 병목 테이블.**

```sql
CREATE TABLE IF NOT EXISTS inventory (
  id         UUID    PRIMARY KEY DEFAULT gen_random_uuid(),
  product_id UUID    NOT NULL REFERENCES products(id),
  size       INTEGER NOT NULL,     -- 신발 사이즈 (240, 250, 260, 270, 280, 290)
  quantity   INTEGER NOT NULL DEFAULT 0,   -- 실제 보유 재고
  reserved   INTEGER NOT NULL DEFAULT 0,   -- 결제 대기 중 예약 수량
  version    INTEGER NOT NULL DEFAULT 0,   -- 낙관적 락 버전 (현재 미사용, 확장 대비)
  UNIQUE (product_id, size)
);
```

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | PK |
| product_id | UUID | FK → products.id |
| size | INTEGER | 신발 사이즈 mm (240~290, 6종) |
| quantity | INTEGER | 실물 재고 수량 |
| reserved | INTEGER | 주문 후 결제 대기 중인 예약 수량 |
| version | INTEGER | 낙관적 락용 버전 (미사용 중, 확장 대비) |

**가용 재고 계산:**
```sql
available = GREATEST(0, quantity - reserved)
```

**재고 상태 분류 (Product Service):**

| 조건 | 상태 표시 |
|------|----------|
| `available = 0` | 품절 |
| `available <= 5` | 재고 부족 |
| `available > 5` | 재고 있음 |

---

### orders

> Order Service 소유. 주문 헤더 정보.

```sql
CREATE TABLE IF NOT EXISTS orders (
  id            UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id       UUID        REFERENCES users(id),
  status        VARCHAR(20) NOT NULL DEFAULT 'PENDING',
  total_price   INTEGER     NOT NULL,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  paid_at       TIMESTAMPTZ,          -- 결제 완료 시각
  failed_at     TIMESTAMPTZ,          -- 결제 실패 시각
  failed_reason TEXT                  -- 실패 사유
);
```

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | PK |
| user_id | UUID | FK → users.id |
| status | VARCHAR(20) | `PENDING` / `PAID` / `FAILED` |
| total_price | INTEGER | 주문 총액 (원) |
| paid_at | TIMESTAMPTZ | PAID 전환 시각 (NULL = 미결제) |
| failed_at | TIMESTAMPTZ | FAILED 전환 시각 |
| failed_reason | TEXT | 실패 사유 |

**주문 상태 전이:**
```
생성 → PENDING
         ├── 결제 성공 → PAID
         └── 결제 실패 → FAILED
```

---

### order_items

> Order Service 소유. 주문 상세 항목.

```sql
CREATE TABLE IF NOT EXISTS order_items (
  id           UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
  order_id     UUID         NOT NULL REFERENCES orders(id),
  product_id   UUID         NOT NULL,
  product_name VARCHAR(255) NOT NULL,    -- 주문 시점 상품명 스냅샷
  size         INTEGER      NOT NULL,
  quantity     INTEGER      NOT NULL,
  unit_price   INTEGER      NOT NULL    -- 주문 시점 가격 스냅샷
);
```

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | PK |
| order_id | UUID | FK → orders.id |
| product_id | UUID | 상품 UUID (products 테이블 직접 FK 없음 — 서비스 분리 원칙) |
| product_name | VARCHAR(255) | 주문 당시 상품명 스냅샷 (이후 상품명 변경 무관) |
| size | INTEGER | 선택 사이즈 |
| quantity | INTEGER | 수량 |
| unit_price | INTEGER | 주문 당시 단가 스냅샷 |

---

### payments

> Payment Service 소유. 결제 처리 결과.

```sql
CREATE TABLE IF NOT EXISTS payments (
  id            UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  order_id      UUID        NOT NULL REFERENCES orders(id),
  method        VARCHAR(20) NOT NULL DEFAULT 'card',  -- card | bank
  status        VARCHAR(20) NOT NULL,                 -- PAID | FAILED
  amount        INTEGER     NOT NULL,
  failed_reason TEXT,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | PK |
| order_id | UUID | FK → orders.id |
| method | VARCHAR(20) | `card` / `bank` |
| status | VARCHAR(20) | `PAID` / `FAILED` |
| amount | INTEGER | 결제 금액 (원) |
| failed_reason | TEXT | `PAYMENT_GATEWAY_ERROR` / `INSUFFICIENT_STOCK` / `INVENTORY_DEDUCT_FAILED` |
| created_at | TIMESTAMPTZ | 결제 처리 시각 |

---

## 3. ERD

```
┌─────────────┐         ┌──────────────────┐
│    users    │         │     products     │
├─────────────┤         ├──────────────────┤
│ id (PK)     │         │ id (PK)          │
│ email       │         │ name             │
│ password    │         │ price            │
│ name        │         │ is_timesale      │
│ created_at  │         │ sale_price       │
└──────┬──────┘         │ sale_ends_at     │
       │                └────────┬─────────┘
       │ 1:N                     │ 1:N
       ▼                         ▼
┌─────────────┐         ┌──────────────────┐
│   orders    │         │    inventory     │
├─────────────┤         ├──────────────────┤
│ id (PK)     │         │ id (PK)          │
│ user_id(FK) │         │ product_id (FK)  │
│ status      │         │ size             │
│ total_price │         │ quantity         │
│ paid_at     │         │ reserved         │
└──────┬──────┘         └──────────────────┘
       │
       ├─── 1:N ──────────────────────────┐
       │                                  │
       ▼                                  ▼
┌──────────────────┐         ┌──────────────────┐
│   order_items    │         │    payments      │
├──────────────────┤         ├──────────────────┤
│ id (PK)          │         │ id (PK)          │
│ order_id (FK)    │         │ order_id (FK)    │
│ product_id       │         │ method           │
│ product_name     │         │ status           │
│ size             │         │ amount           │
│ quantity         │         │ failed_reason    │
│ unit_price       │         └──────────────────┘
└──────────────────┘
```

---

## 4. 인덱스 전략

```sql
-- 재고 조회 (product_id로 size별 재고 조회 — 가장 빈번)
CREATE INDEX idx_inventory_product_id  ON inventory(product_id);

-- 주문 상세 조회 (order_id로 items 조회)
CREATE INDEX idx_order_items_order_id  ON order_items(order_id);

-- 주문 상태 필터 (status별 집계 쿼리)
CREATE INDEX idx_orders_status         ON orders(status);

-- 사용자별 주문 조회
CREATE INDEX idx_orders_user_id        ON orders(user_id);

-- 결제 내역 조회 (order_id로 payment 조회)
CREATE INDEX idx_payments_order_id     ON payments(order_id);

-- 타임세일 상품 필터 (is_timesale = TRUE 조회 — 어드민 페이지, 캐시 무효화 후)
CREATE INDEX idx_products_is_timesale  ON products(is_timesale);
```

| 인덱스 | 사용 쿼리 | 효과 |
|--------|----------|------|
| `idx_inventory_product_id` | `WHERE product_id = $1 AND size = $2` | 재고 예약/차감 시 행 빠른 탐색 |
| `idx_products_is_timesale` | `WHERE is_timesale = TRUE` | 타임세일 시작 후 목록 캐시 미스 시 풀스캔 방지 |
| `idx_orders_status` | `WHERE status = 'PENDING'` | 미결제 주문 집계 |
| `idx_payments_order_id` | `WHERE order_id = $1` | 결제 상태 조회 |

---

## 5. 서비스별 핵심 쿼리

### User Service

```sql
-- 로그인 (email로 사용자 조회 → bcrypt 검증)
SELECT id, email, password, name
FROM users
WHERE email = $1;

-- 회원가입
INSERT INTO users (email, password, name)
VALUES ($1, $2, $3)
RETURNING id, email, name;
```

### Product Service

```sql
-- 상품 목록 (Redis 캐시 미스 시)
SELECT p.id, p.name, p.price, p.description, p.image_url,
       p.is_timesale, p.sale_price, p.sale_ends_at,
       COALESCE(SUM(GREATEST(0, i.quantity - i.reserved)), 0) AS total_available
FROM products p
LEFT JOIN inventory i ON i.product_id = p.id
GROUP BY p.id
ORDER BY p.created_at DESC;

-- 타임세일 시작 (무작위 N개 선택, 할인율 적용)
UPDATE products
SET is_timesale  = TRUE,
    sale_price   = ROUND(price * (1 - $1 / 100.0)),
    sale_ends_at = NOW() + ($2 || ' hours')::INTERVAL
WHERE id IN (
  SELECT id FROM products
  WHERE is_timesale = FALSE
  ORDER BY RANDOM()
  LIMIT $3
);

-- 타임세일 전체 종료
UPDATE products
SET is_timesale  = FALSE,
    sale_price   = NULL,
    sale_ends_at = NULL
WHERE is_timesale = TRUE;
```

### Inventory Service

```sql
-- 가용 재고 조회
SELECT
  SUM(GREATEST(0, quantity - reserved)) AS available,
  SUM(quantity)                         AS total,
  SUM(reserved)                         AS reserved
FROM inventory
WHERE product_id = $1;

-- 재고 예약 (SELECT FOR UPDATE — 아래 6절에서 상세 설명)
BEGIN;
SELECT quantity, reserved FROM inventory
WHERE product_id = $1 AND size = $2
FOR UPDATE;
-- available 검증 후
UPDATE inventory
SET reserved = reserved + $3
WHERE product_id = $1 AND size = $2;
COMMIT;

-- 재고 실차감 (결제 성공 시)
UPDATE inventory
SET quantity = quantity - $3,
    reserved = reserved - $3
WHERE product_id = $1 AND size = $2;

-- 재고 예약 해제 (결제 실패 시)
UPDATE inventory
SET reserved = reserved - $3
WHERE product_id = $1 AND size = $2;
```

### Order Service

```sql
-- 주문 생성
INSERT INTO orders (user_id, status, total_price)
VALUES ($1, 'PENDING', $2)
RETURNING id;

-- 주문 상세 항목 삽입
INSERT INTO order_items (order_id, product_id, product_name, size, quantity, unit_price)
VALUES ($1, $2, $3, $4, $5, $6);

-- 주문 + items 조회 (Payment Service가 호출)
SELECT o.id, o.status, o.total_price,
       json_agg(json_build_object(
         'product_id',   oi.product_id,
         'product_name', oi.product_name,
         'size',         oi.size,
         'quantity',     oi.quantity
       )) AS items
FROM orders o
JOIN order_items oi ON oi.order_id = o.id
WHERE o.id = $1
GROUP BY o.id;

-- 주문 상태 업데이트
UPDATE orders
SET status    = $2,
    paid_at   = CASE WHEN $2 = 'PAID'   THEN NOW() END,
    failed_at = CASE WHEN $2 = 'FAILED' THEN NOW() END,
    failed_reason = $3
WHERE id = $1;
```

### Payment Service

```sql
-- 결제 저장
INSERT INTO payments (id, order_id, method, status, amount, failed_reason)
VALUES ($1, $2, $3, $4, $5, $6);

-- 실시간 통계 (Redis 캐시 미스 시, 3초 TTL)
SELECT
  COUNT(*)::int                                   AS total,
  COUNT(*) FILTER (WHERE status = 'PAID')::int   AS success,
  COUNT(*) FILTER (WHERE status = 'FAILED')::int AS failed
FROM payments;

-- 최근 15건 결제 내역
SELECT
  p.created_at                                                  AS time,
  oi.product_name                                               AS product,
  oi.size::text                                                 AS size,
  CASE WHEN p.status = 'PAID' THEN 'SUCCESS' ELSE 'FAILED' END AS status,
  p.failed_reason                                               AS reason
FROM payments p
LEFT JOIN LATERAL (
  SELECT product_name, size FROM order_items
  WHERE order_id = p.order_id ORDER BY id LIMIT 1
) oi ON true
ORDER BY p.created_at DESC
LIMIT 15;
```

---

## 6. 동시성 처리 — SELECT FOR UPDATE

### 왜 필요한가

타임세일 시 수백 명이 동시에 동일 상품·사이즈에 주문을 넣으면,  
재고 확인 → UPDATE 사이에 다른 트랜잭션이 끼어들어 **재고 초과 판매**가 발생한다.

```
[동시 요청 100개]
T1: SELECT quantity=10, reserved=8 → available=2 → UPDATE reserved=9
T2: SELECT quantity=10, reserved=8 → available=2 → UPDATE reserved=9  ← 동시에 읽어서 같은 값
결과: reserved=9 (2번 실행됐지만 9로 덮어씀) → 데이터 불일치
```

### SELECT FOR UPDATE 동작

```sql
BEGIN;

-- 해당 행에 배타적 락(X-Lock) 획득
-- 다른 트랜잭션은 COMMIT 전까지 이 행을 읽거나 수정할 수 없음
SELECT quantity, reserved
FROM inventory
WHERE product_id = $1 AND size = $2
FOR UPDATE;

-- 가용 재고 검증
-- available = GREATEST(0, quantity - reserved)
-- available < 요청수량이면 ROLLBACK → 409 반환

-- 예약 수량 증가
UPDATE inventory
SET reserved = reserved + $3
WHERE product_id = $1 AND size = $2;

COMMIT;  -- 락 해제 → 다음 대기 트랜잭션이 락 획득
```

### 실험에서의 의미

```
동시 요청 급증
    ↓
SELECT FOR UPDATE 락 대기 누적
    ↓
DB 커넥션 큐 포화
    ↓
응답 시간 급증 (P95 Latency)
    ↓
timeout → 5xx Error Rate 상승
```

이 과정이 **온프레미스에서 더 심각**하게 나타난다:
- 온프레미스: 파드 수 제한 → 동시 처리 커넥션 수 제한 → 락 대기 길어짐
- EKS: Karpenter 노드 추가 → 파드 증가 → 커넥션 풀 확장 → 락 대기 분산

---

## 7. Redis 캐싱 구조

### 캐시 키 목록

| 키 패턴 | TTL | 저장 내용 | 무효화 시점 |
|---------|-----|----------|------------|
| `products:list` | 60s | 전체 상품 목록 (total_available 포함) | 타임세일 시작/종료 시 즉시 DEL |
| `products:{uuid}` | 30s | 상품 상세 | 타임세일 시작/종료 시 즉시 DEL |
| `stats:realtime` | 3s | 결제 통계 (total/success/failed/rate/recent15) | 결제 처리 완료 시 즉시 DEL |

### 캐시 히트/미스 흐름

```
GET /api/products
  → redis.get('products:list')
      ↓ HIT  → JSON.parse → 즉시 반환 (DB 쿼리 0)
      ↓ MISS → DB SELECT (JOIN inventory) → redis.setex(60) → 반환

POST /api/products/timesale/start (타임세일 시작)
  → DB UPDATE (products)
  → redis.del('products:list')        ← 즉시 무효화
  → redis.del('products:{id}') × N개  ← 변경된 상품 모두 무효화
```

### 재고는 절대 캐시하지 않는 이유

```
재고 캐시 HIT → 실제 재고와 다른 값 반환
             → available 검증 오류
             → 초과 예약 or 재고 있는데 품절 표시
             → 실험 결과 오염 (SELECT FOR UPDATE 의미 없어짐)
```

---

## 8. 온프레미스 구축 — EC2 + Docker

### EC2 스펙

| 항목 | 값 |
|------|-----|
| 인스턴스 타입 | t3.medium |
| OS | Ubuntu 22.04 또는 24.04 |
| 위치 | VPC-A, Public 서브넷 (10.0.0.0/16) |
| 역할 | PostgreSQL 전용 (Redis는 별도 t3.micro) |

### 1. Docker 설치

```bash
sudo apt update && sudo apt install -y docker.io
sudo systemctl enable --now docker
sudo usermod -aG docker ubuntu
newgrp docker  # 또는 재로그인
```

### 2. schema.sql, seed.sql 전송

```bash
# 로컬 Mac에서 실행
scp -i ~/key/aws-3tier-keypair.pem \
  /Users/kimminseo/shopping_k8s/msa_shoply/db/schema.sql \
  /Users/kimminseo/shopping_k8s/msa_shoply/db/seed.sql \
  ubuntu@<PostgreSQL-EC2-공인IP>:~/
```

### 3. PostgreSQL 컨테이너 실행

```bash
# PostgreSQL EC2에서 실행
docker run -d \
  --name postgres \
  --restart always \
  -e POSTGRES_DB=shoply \
  -e POSTGRES_USER=shoply \
  -e POSTGRES_PASSWORD=shoply1234 \
  -v postgres_data:/var/lib/postgresql/data \
  -v ~/schema.sql:/docker-entrypoint-initdb.d/01_schema.sql \
  -v ~/seed.sql:/docker-entrypoint-initdb.d/02_seed.sql \
  -p 5432:5432 \
  postgres:16-alpine
```

> `docker-entrypoint-initdb.d/`에 마운트된 `.sql` 파일은  
> 컨테이너 **최초 기동 시 한 번만** 자동 실행된다.  
> 볼륨에 데이터가 이미 있으면 실행되지 않음.

### 4. 데이터 적재 확인

```bash
docker exec -it postgres psql -U shoply -d shoply -c "\dt"
docker exec -it postgres psql -U shoply -d shoply \
  -c "SELECT COUNT(*) FROM products;"
# 500개 확인
```

### 5. 보안그룹 설정 (PostgreSQL SG)

| 방향 | 포트 | 소스 | 이유 |
|------|------|------|------|
| 인바운드 | 5432 | c8i.2xlarge SG | k8s 파드 → DB 연결 |
| 인바운드 | 9187 | Monitoring SG | postgresql_exporter scrape |
| 인바운드 | 9100 | Monitoring SG | node_exporter scrape |
| 인바운드 | 22 | Admin IP/32 | SSH 접근 |
| 아웃바운드 | 전체 | 0.0.0.0/0 | — |

### 6. k8s ConfigMap에 IP 입력

`msa_shoply/k8s/onprem/configmap-patch.yaml` :

```yaml
POSTGRES_HOST: "10.0.2.128"   # 실제 PostgreSQL EC2 사설 IP로 변경
POSTGRES_PORT: "5432"
REDIS_HOST: "10.0.6.15"       # 실제 Redis EC2 사설 IP로 변경
REDIS_PORT: "6379"
```

---

## 9. EKS 구축 — RDS

### RDS 생성 (AWS 콘솔)

| 항목 | 값 |
|------|-----|
| 엔진 유형 | PostgreSQL |
| 엔진 버전 | 16.x |
| 템플릿 | 프리티어 (실험용) |
| DB 인스턴스 클래스 | db.t3.medium |
| 스토리지 | 20GB gp2 |
| DB 인스턴스 식별자 | shoply-db |
| 마스터 사용자 이름 | shoply |
| 마스터 암호 | shoply1234 |
| VPC | EKS VPC (VPC-B, 10.1.0.0/16) |
| 퍼블릭 액세스 | 아니요 |
| VPC 보안 그룹 | RDS SG 신규 생성 |

### 스키마 + 시드 데이터 적용

RDS는 VPC 내부에서만 접근 가능하므로 EKS Worker Node 또는 Bastion에서 실행한다.

```bash
# Worker Node 또는 Bastion에서
# psql 설치
sudo apt install -y postgresql-client

# 스키마 적용
psql -h <RDS-엔드포인트> -U shoply -d shoply \
  -f /path/to/schema.sql

# 시드 데이터 적용 (44,000줄 — 수분 소요)
psql -h <RDS-엔드포인트> -U shoply -d shoply \
  -f /path/to/seed.sql

# 확인
psql -h <RDS-엔드포인트> -U shoply -d shoply \
  -c "SELECT COUNT(*) FROM products;"
# 500개 확인
```

### EKS ConfigMap에 엔드포인트 입력

`msa_shoply/k8s/eks/configmap-patch.yaml` :

```yaml
POSTGRES_HOST: "shoply-db.xxxx.ap-northeast-2.rds.amazonaws.com"
POSTGRES_PORT: "5432"
REDIS_HOST: "<Redis-EC2-사설IP>"
REDIS_PORT: "6379"
```

### RDS 보안그룹 설정

| 방향 | 포트 | 소스 | 이유 |
|------|------|------|------|
| 인바운드 | 5432 | Worker Node SG | k8s 파드 → RDS |

> postgresql_exporter는 EKS 내부 Pod으로 배포 → RDS 접근 후 NodePort 노출  
> Prometheus는 Worker Node NodePort를 scrape

### 온프레미스 vs RDS 차이

| 항목 | 온프레미스 (Docker EC2) | EKS (RDS) |
|------|----------------------|-----------|
| 장애 복구 | 수동 재시작 필요 | Multi-AZ 자동 페일오버 |
| 백업 | 직접 설정 | 자동 백업 |
| max_connections | `postgresql.conf` 수동 수정 | RDS 파라미터 그룹으로 관리 |
| 패치/업그레이드 | 직접 | AWS 자동 (유지 관리 창) |
| 모니터링 | node_exporter + pg_exporter | CloudWatch + pg_exporter |

---

## 10. 시드 데이터

### 구성 요약

| 테이블 | 건수 | 내용 |
|--------|------|------|
| users | 약 500개 | admin 1 + test 계정 10 + Bedrock 생성 계정 |
| products | 500개 | 신발 상품 (영문 이름, 원 단위 가격) |
| inventory | 3,000개 | 상품당 6사이즈(240~290), 초기 quantity=100, reserved=0 |

### 핵심 계정

| 이메일 | 비밀번호 | 역할 |
|--------|---------|------|
| `admin@shoply.com` | `Admin1234!` | 어드민 — 타임세일 제어 페이지 접근 가능 |
| `test1@shoply.com` | `Test1234!` | 일반 사용자 |
| `test2@shoply.com` | `Test1234!` | 일반 사용자 |
| … | … | … |
| `test10@shoply.com` | `Test1234!` | 일반 사용자 |

> 전체 계정 목록 (비밀번호 포함): `msa_shoply/db/test_accounts.txt`

### 시드 데이터 재생성

```bash
cd msa_shoply/db
node generate-seed.js
# AWS Bedrock (Claude 3.5 Sonnet) 호출로 상품 데이터 생성
# 완료 후 seed.sql 파일 덮어쓰기
```

---

## 11. 실험 전 초기화 체크리스트

### PostgreSQL 상태 확인

```sql
-- 1. reserved 잔여 확인 (0이어야 함 — 이전 실험 찌꺼기 확인)
SELECT COUNT(*) AS dirty_rows
FROM inventory
WHERE reserved != 0;

-- 2. 주문 상태 분포
SELECT status, COUNT(*) FROM orders GROUP BY status;

-- 3. 결제 건수 확인
SELECT status, COUNT(*) FROM payments GROUP BY status;

-- 4. 타임세일 상태 확인
SELECT COUNT(*) FROM products WHERE is_timesale = TRUE;
-- 실험 시작 전: 0이어야 함
```

### 완전 초기화 (실험 재시작 시)

```bash
# 온프레미스 Docker
docker exec -it postgres psql -U shoply -d shoply << 'EOF'
TRUNCATE payments, order_items, orders RESTART IDENTITY CASCADE;
UPDATE inventory SET reserved = 0;
UPDATE products SET is_timesale = FALSE, sale_price = NULL, sale_ends_at = NULL;
EOF

# RDS (Bastion 또는 Worker Node에서)
psql -h <RDS-엔드포인트> -U shoply -d shoply << 'EOF'
TRUNCATE payments, order_items, orders RESTART IDENTITY CASCADE;
UPDATE inventory SET reserved = 0;
UPDATE products SET is_timesale = FALSE, sale_price = NULL, sale_ends_at = NULL;
EOF
```

### Redis 초기화

```bash
# 온프레미스
docker exec -it redis redis-cli FLUSHALL

# EKS (Redis EC2에서)
redis-cli FLUSHALL

# 확인
redis-cli DBSIZE
# (integer) 0
```

---

## 12. 로컬 개발 환경 검증

> 로컬 Mac에서 Docker Compose로 개발할 때의 검증 절차.  
> 작업 디렉토리: `shopping_k8s/msa_shoply/`

### 빠른 체크리스트

```
□ msa_postgres  — Up (healthy)
□ msa_redis     — Up (healthy)
□ users         — 1,011건 이상
□ products      — 500건
□ inventory     — 3,000건 이상
□ reserved      — 전부 0 (선점 없음)
□ Redis ping    — PONG
□ Redis dbsize  — 0 (서비스 기동 전)
```

### PostgreSQL + Redis 기동

```bash
cd msa_shoply
docker compose up -d postgres redis

# 상태 확인
docker compose ps
```

**정상 상태:**
```
NAME           IMAGE                STATUS
msa_postgres   postgres:16-alpine   Up (healthy)
msa_redis      redis:7-alpine       Up (healthy)
```

> `docker-entrypoint-initdb.d/` 안의 SQL은 컨테이너 **최초 기동 시 한 번만** 실행.  
> 볼륨이 이미 있으면 재실행되지 않음 → 재초기화하려면 `docker compose down -v` 후 재기동.

### 초기화 로그 확인

```bash
docker logs msa_postgres 2>&1 | grep -iE "(seed|schema|error|fatal)"
# 정상: 에러 없이 조용히 종료
# 비정상: ERROR / FATAL 메시지 → 트러블슈팅 참고
```

### psql 접속

```bash
# 컨테이너 내부
docker exec -it msa_postgres psql -U shoply -d shoply

# 로컬 psql 설치된 경우
psql -h localhost -p 5432 -U shoply -d shoply
# 비밀번호: shoply1234
```

### 데이터 건수 검증

```sql
SELECT tbl, cnt FROM (
  SELECT 'users'       AS tbl, COUNT(*) AS cnt FROM users       UNION ALL
  SELECT 'products',                COUNT(*)        FROM products    UNION ALL
  SELECT 'inventory',               COUNT(*)        FROM inventory   UNION ALL
  SELECT 'orders',                  COUNT(*)        FROM orders      UNION ALL
  SELECT 'order_items',             COUNT(*)        FROM order_items UNION ALL
  SELECT 'payments',                COUNT(*)        FROM payments
) t ORDER BY tbl;
```

### 재고 선점 상태 확인 (실험 전 반드시 0)

```sql
SELECT COUNT(*) AS has_reserved FROM inventory WHERE reserved != 0;
-- 기대: 0
```

### 고정 계정 확인

```sql
SELECT email FROM users WHERE email LIKE '%@shoply.com' ORDER BY email;
-- admin@shoply.com + test1~test10@shoply.com = 11건
```

### Redis 검증

```bash
docker exec -it msa_redis redis-cli ping
# PONG

docker exec -it msa_redis redis-cli dbsize
# 0 (서비스 기동 전)
```

### 데이터 초기화 방법

```bash
# 방법 A — seed만 재적용 (빠름, 기존 데이터 덮어씌움)
docker exec -i msa_postgres psql -U shoply -d shoply < db/seed.sql

# 방법 B — 완전 재시작 (깔끔)
docker compose down -v
docker compose up -d postgres redis
```

### 트러블슈팅

| 증상 | 원인 | 해결 |
|---|---|---|
| `msa_postgres` unhealthy | 포트 5432 충돌 | `lsof -i :5432` 로 프로세스 확인 후 종료 |
| `msa_redis` unhealthy | 포트 6379 충돌 | `lsof -i :6379` 로 프로세스 확인 후 종료 |
| 볼륨 있어서 seed 미적용 | 기존 볼륨 잔존 | `docker compose down -v` 후 재시작 |
| `reserved != 0` 건 존재 | 이전 실험 잔존 | 방법 A로 seed 재적용 |
| Redis `dbsize` 0이 아님 | 이전 캐시 잔존 | `docker exec -it msa_redis redis-cli flushall` |

---

## 13. 관리 쿼리 모음

### 재고 현황

```sql
-- 전체 재고 현황 요약
SELECT
  COUNT(*)                                      AS total_rows,
  SUM(quantity)                                 AS total_quantity,
  SUM(reserved)                                 AS total_reserved,
  SUM(GREATEST(0, quantity - reserved))         AS total_available,
  COUNT(*) FILTER (WHERE quantity - reserved <= 0) AS out_of_stock
FROM inventory;

-- 상품별 재고 상태 (가용 재고 기준)
SELECT
  p.name,
  SUM(i.quantity)                         AS total_qty,
  SUM(i.reserved)                         AS total_reserved,
  SUM(GREATEST(0, i.quantity - i.reserved)) AS available
FROM products p
JOIN inventory i ON i.product_id = p.id
GROUP BY p.id, p.name
ORDER BY available ASC
LIMIT 20;
```

### 타임세일 현황

```sql
-- 현재 진행 중인 타임세일
SELECT id, name, price, sale_price,
       ROUND((1 - sale_price::numeric / price) * 100) AS discount_pct,
       sale_ends_at
FROM products
WHERE is_timesale = TRUE
ORDER BY sale_ends_at;

-- 타임세일 강제 전체 종료
UPDATE products
SET is_timesale = FALSE, sale_price = NULL, sale_ends_at = NULL
WHERE is_timesale = TRUE;
```

### 결제 통계

```sql
-- 전체 결제 성공률
SELECT
  COUNT(*)::int                                   AS total,
  COUNT(*) FILTER (WHERE status = 'PAID')::int   AS paid,
  COUNT(*) FILTER (WHERE status = 'FAILED')::int AS failed,
  ROUND(
    COUNT(*) FILTER (WHERE status = 'PAID') * 100.0 / NULLIF(COUNT(*), 0),
    1
  )                                               AS success_rate_pct
FROM payments;

-- 실패 사유별 분류
SELECT failed_reason, COUNT(*) AS cnt
FROM payments
WHERE status = 'FAILED'
GROUP BY failed_reason
ORDER BY cnt DESC;

-- 시간대별 결제 건수 (1분 단위)
SELECT
  date_trunc('minute', created_at) AS minute,
  COUNT(*) FILTER (WHERE status = 'PAID')   AS paid,
  COUNT(*) FILTER (WHERE status = 'FAILED') AS failed
FROM payments
GROUP BY 1
ORDER BY 1;
```

### DB 커넥션 모니터링

```sql
-- 현재 활성 커넥션 수 (실험 중 DB 부하 확인)
SELECT COUNT(*), state
FROM pg_stat_activity
WHERE datname = 'shoply'
GROUP BY state;

-- 락 대기 중인 쿼리 (SELECT FOR UPDATE 병목 확인)
SELECT pid, wait_event_type, wait_event, query
FROM pg_stat_activity
WHERE wait_event_type = 'Lock'
  AND datname = 'shoply';

-- 가장 느린 쿼리 Top 10
SELECT query, calls, mean_exec_time, total_exec_time
FROM pg_stat_statements
WHERE dbid = (SELECT oid FROM pg_database WHERE datname = 'shoply')
ORDER BY mean_exec_time DESC
LIMIT 10;
```

---

*Shoply MSA — 데이터베이스 가이드*  
*PostgreSQL 16 + Redis 7 | 온프레미스 Docker EC2 / AWS RDS*
