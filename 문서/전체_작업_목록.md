# 전체 작업 목록 — 체크리스트

> 이 문서는 **"할 일 추적"** 용이다. 상세 방법은 각 전담 가이드를 참조한다.
>
> | 영역 | 전담 문서 |
> |---|---|
> | 인프라 구축 (VPC/SG/k8s/트러블슈팅) | [인프라_가이드.md](인프라_가이드.md) |
> | DB (스키마/쿼리/시드/구축) | [데이터베이스_가이드.md](데이터베이스_가이드.md) |
> | 모니터링 (Prometheus/Grafana/패널) | [모니터링.md](모니터링.md) |
> | 실험 (프레임/시나리오/설정값/측정) | [비교실험_가이드.md](비교실험_가이드.md) |
> | 서비스 테스트 (curl) | [테스트.md](테스트.md) |

---

## 1. 결정 완료 ✅

| 항목 | 결정 | 상세 |
|---|---|---|
| 계정/VPC | 단일 계정, VPC 2개 (온프레+공통 / EKS) | 인프라_가이드 |
| 레지스트리 | **GHCR 단일** (ECR 미사용) | — |
| 부하 도구 | **k6** | — |
| 부하 방식 | **하이브리드** (request 튜닝 + 큰 RPS, 부하코드 X) | 비교실험_가이드 §1 |
| 실험 프레임 | HPA만(온프레) vs HPA+Karpenter(EKS) | 비교실험_가이드 §0 |
| 시나리오 | **A 한계 / B 노드장애 / C DB페일오버** | 비교실험_가이드 §4 |
| 트래픽 진입 | Nginx Ingress (NodePort :30080) | 인프라_가이드 §4 |
| 노드 가상화 | **LXD 컨테이너** (c8i KVM 미지원) | — |

---

## 2. 앱·코드 ✅ (이번 정리 완료)

- [x] 5개 서비스 부하코드 제거 (compression / jwt.verify / LRU / bcrypt cost12)
- [x] checkout 담기 버그 수정
- [x] DB 커넥션 풀 8 통일 + 페일오버 설정 (connectionTimeout / keepAlive)
- [x] 리소스 100m/500m 통일 + HPA target 60% 통일
- [x] soft nodeAffinity (worker1/worker2)
- [x] 전체 빌드 검증 통과
- [x] dead code 정리 (gateway auth.ts / compression 의존성)
- [x] Payment Service 구현
- [ ] **이미지 `:latest` → 커밋 SHA 고정** (본 실험 직전)

---

## 3. 온프레미스 인프라 ⬜ → [인프라_가이드.md](인프라_가이드.md)

- [ ] VPC-A + EC2 (c8i / PostgreSQL / Redis / Monitoring / Locust)
- [ ] LXD 컨테이너 3개 (master + worker1/2)
- [ ] k8s 클러스터 (kubeadm, Flannel, v1.34.x)
- [ ] **노드 라벨링** `experiment-role=worker1/worker2` ← affinity 동작 전제
- [ ] Metrics Server / Nginx Ingress / kube-state-metrics / node_exporter
- [ ] EC2 호스트 Nginx (NAT → 컨테이너 :30080) + iptables DNAT
- [ ] GHCR imagePullSecret
- [ ] 서비스 배포 (Deployment / HPA / Ingress)

---

## 4. EKS 인프라 ⬜ → [인프라_가이드.md](인프라_가이드.md)

- [ ] VPC-B + EKS 클러스터 (온프레와 동일 버전)
- [ ] Karpenter (NodePool / EC2NodeClass)
- [ ] kube-state-metrics / node_exporter
- [ ] RDS PostgreSQL 16 (Multi-AZ ← 시나리오 C)
- [ ] Redis EC2
- [ ] 서비스 배포 (동일 이미지)

---

## 5. 모니터링 ⬜ → [모니터링.md](모니터링.md)

- [ ] Monitoring EC2 — Prometheus + Grafana (docker compose)
- [ ] prometheus.yml scrape 타겟 (온프레/EKS)
- [ ] Grafana 대시보드 import (`grafana-dashboard-onprem.json` + EKS용)
- [ ] OpenSearch + Fluent Bit (로그 원인 분석)

---

## 6. CI/CD

- [x] CI (PR: 변경 서비스만 tsc + audit + 빌드)
- [x] CD (main: GHCR push + Trivy)
- [ ] 실험 배포 시 **SHA 태그 고정** 반영
- [ ] (선택) 배포 자동화 — 단 클러스터 내부 툴은 미사용 (실험 오염 방지)

---

## 7. 실험 ⬜ → [비교실험_가이드.md](비교실험_가이드.md)

- [ ] **DB Primary-Replica 구성** (시나리오 C용)
- [ ] **k6 A/B/C 스크립트 분리 작성** (현재 `scenario.js`는 검증용)
- [ ] 리허설 (시나리오 A 500 RPS → request/RPS 다이얼 확정)
- [ ] 본 실험: A → B → C
- [ ] 결과 정리 (한계 RPS / MTTR 비교)

---

## 8. 현재 상태 & 다음 할 일

**온프레미스 완료:**
- KVM K8s 클러스터 (master + worker1 + worker2, 각 2core/4GB)
- MSA 7개 서비스 배포 (replicas: 1, HPA maxReplicas: 99)
- metrics-server, kube-state-metrics 설치
- Prometheus + Grafana 모니터링 (LXD 제거 → KVM node_exporter)
- iptables 포워딩 (메트릭 포트 30400~30404, 30800, 39101, 39102)
- k6 부하 테스트 (setup() 고정 토큰, 웨이브 2000명)
- inventory SELECT FOR UPDATE (DB lock 경합 → CPU 스파이크 → 스케일)
- Grafana 대시보드 (서비스별 RPS/P95, Pending Pod, HPA, 워커 CPU/메모리)

**다음 우선순위:**
1. 🔴 EKS 구축 (병렬 가능)
2. 🔴 EKS prometheus.yml 업데이트 및 스크레이프 연결
3. 🔴 온프레미스 최종 부하 데이터 수집
4. 🟡 k6 A/B/C 시나리오 스크립트 분리
5. 🟢 본 실험 A → B → C 결과 정리
