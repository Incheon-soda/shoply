# 전체 작업 목록 — 온프레미스 vs EKS 비교 실험

> 이 문서는 "무엇을 결정해야 하는가"와 "무엇을 해야 하는가"를 전부 뽑은 것이다.  
> 먼저 결정 사항을 정리하고, 그 다음 환경별 작업 목록으로 이어진다.

---

## 목차

1. [먼저 결정해야 할 것들](#1-먼저-결정해야-할-것들)
2. [온프레미스 k8s — 전체 작업](#2-온프레미스-k8s--전체-작업)
3. [EKS — 전체 작업](#3-eks--전체-작업)
4. [공통 인프라 — 전체 작업](#4-공통-인프라--전체-작업)
5. [앱·서비스 배포 준비](#5-앱서비스-배포-준비)
6. [CI/CD 구성](#6-cicd-구성)
7. [실험 진행 체크리스트](#7-실험-진행-체크리스트)
8. [현재 상태 요약](#8-현재-상태-요약)

---

## 1. 먼저 결정해야 할 것들

작업을 시작하기 전에 아래 항목들을 팀이 합의해야 한다.  
결정이 안 되면 이후 작업이 충돌한다.

---

### 1-1. VPC 설계 — 몇 개? 어떻게?

> ✅ **결정: 단일 계정 우선, 크레딧 부족 시 2계정으로 전환**

**기본 구조: 단일 계정 + VPC 2개**

```
단일 AWS 계정

VPC-A (10.0.0.0/16) — 온프레미스 + 공통
└── Public 서브넷 (10.0.1.0/24) — ap-northeast-2a
    ├── c8i.2xlarge   (k8s 호스트 + KVM)
    ├── t3.medium     (PostgreSQL)
    ├── t3.micro      (Redis)
    ├── t3.small      (Monitoring — Prometheus + Grafana)
    └── t3.medium     (Locust)

VPC-B (10.1.0.0/16) — EKS
├── Public 서브넷 ap-northeast-2a  (10.1.1.0/24)
├── Public 서브넷 ap-northeast-2b  (10.1.2.0/24)  ← EKS 최소 2개 AZ 필요
├── EKS 워커노드 t3.medium × 2
├── RDS db.t3.medium
└── t3.micro (Redis)
```

**Prometheus scrape 전략 (VPC 구분 없이 동작하도록):**  
처음부터 Public IP + NodePort로 설계 → 나중에 2계정으로 분리해도 scrape 설정 변경 없음

```
Monitoring EC2 (VPC-A)
  ├── 온프레미스 k8s: c8i.2xlarge 공인IP:NodePort  (VPC 내부 → 사설IP 사용 가능)
  ├── EKS 워커노드:   공인IP:NodePort               (VPC-B → 공인IP 경유)
  ├── PostgreSQL:    VPC-A 사설IP:9187
  └── Redis × 2:    각각 사설IP or 공인IP:9121
```

**2계정 전환 시 바뀌는 것 (최소화):**
- Monitoring EC2 EIP를 EKS 워커노드 SG에 추가 등록
- ECR 각 계정에 따로 push (GitHub Actions credentials 추가)
- 그 외 scrape 설정, k8s yaml은 그대로

---

### 1-2. Prometheus 아키텍처

> ✅ **결정: 계정 구조 무관하게 Prometheus 1개 (VPC-A t3.small)**

```
Monitoring EC2 (VPC-A, t3.small, EIP 고정)
  │
  ├── 온프레미스 scrape
  │     ├── kube-state-metrics   c8i.2xlarge 사설IP:NodePort   (단일계정 내부)
  │     ├── prom-client 각 서비스 c8i.2xlarge 사설IP:NodePort
  │     ├── PostgreSQL Exporter  t3.medium 사설IP:9187
  │     ├── Redis Exporter       t3.micro  사설IP:9121
  │     └── Node Exporter        VM 192.168.122.2~4:9100
  │           ↑ 단일계정: 사설IP / 2계정: 공인IP로 변경
  │
  └── EKS scrape
        ├── kube-state-metrics   EKS Worker Node 공인IP:NodePort
        ├── prom-client 각 서비스 EKS Worker Node 공인IP:NodePort
        ├── postgresql_exporter  EKS 내부 k8s Pod → Worker Node 공인IP:NodePort
        ├── Redis Exporter       EKS Redis EC2 공인IP:9121
        └── Node Exporter        EKS Worker Node 공인IP:9100

Grafana (같은 t3.small, 포트 3001)
  └── DataSource: Prometheus 1개
      └── 온프레미스 | EKS 패널 나란히 → 동일 타임라인 비교
```

**EKS postgresql_exporter 배포 방식:**  
RDS는 VPC-B 내부에서만 접근 가능 → exporter를 k8s Pod로 배포 → NodePort로 노출 → Prometheus scrape

**2계정 전환 시 변경:**  
온프레미스 scrape 대상 IP를 사설IP → 공인IP로 변경, Monitoring EIP는 그대로

---

### 1-3. Locust 배치

> ✅ **결정: 계정 구조 무관하게 Locust 2대 (각 환경 전용)**

```
Locust-A (VPC-A, t3.medium, EIP)  → 온프레미스 LB 전용
  타겟: http://<c8i.2xlarge-사설IP>:80   (단일계정: 내부)
        http://<c8i.2xlarge-공인IP>:80   (2계정: 외부)

Locust-B (VPC-A, t3.medium, EIP)  → EKS ALB 전용
  타겟: http://<ALB-DNS>:80              (항상 공인DNS)
```

**왜 2대인가:**  
시나리오 2에서 온프레미스가 응답 지연/Error 급증 → Locust-A 스레드 쌓임.  
1대면 이 지연이 EKS 부하에도 영향 → 실험 결과 오염.  
2대로 분리하면 한쪽이 터져도 다른 쪽 부하는 독립 유지.

**동시 시작: Locust 스케줄 기능**
```bash
# 두 EC2에서 동일 시각에 실행 (예: 14:00:00 정각)
# Locust-A
locust -f locustfile.py --host http://<온프레미스-IP> \
  --headless --users 200 --spawn-rate 50 --run-time 20m

# Locust-B
locust -f locustfile.py --host http://<ALB-DNS> \
  --headless --users 200 --spawn-rate 50 --run-time 20m
```
동일 스크립트를 GitHub 공통 레포에서 pull 후 실행.

**2계정 전환 시 변경:**  
Locust-B EIP → EKS 계정 VPC-B로 이동 or EKS 계정에 새 Locust-B 생성.  
Locust-A 타겟 IP만 사설→공인으로 변경.

---

### 1-4. ECR — 공유 or 각 계정에 따로?

**상황:**  
Docker 이미지는 완전히 동일해야 한다. 어느 레지스트리에서 pull할 것인가?

**옵션 A: 온프레미스 팀 계정 ECR → EKS 팀이 cross-account pull**

```
GitHub Actions → 온프레미스 계정 ECR에 push
EKS 계정: ECR 리소스 정책으로 cross-account pull 허용
```

- 단점: cross-account ECR 정책 설정 복잡

**옵션 B: 각 계정에 ECR, CI/CD가 양쪽에 동시 push**

```
GitHub Actions
  ├── 온프레미스 계정 ECR에 push (tag: v1.0.0)
  └── EKS 계정 ECR에 push (tag: v1.0.0, 동일 이미지)
```

- 장점: 독립적, 각 계정에서 직접 pull
- 단점: GitHub Actions에 두 계정의 IAM credentials 등록 필요

**→ ✅ 결정: 계정 구조에 따라 결정**
- 단일 계정: ECR 1개 공유 (같은 계정이므로 cross-account 불필요)
- 2계정 전환 시: 각 계정 ECR에 동일 이미지 push (GitHub Actions credentials 2개 등록)

---

### 1-5. CNI 선택 (온프레미스)

| CNI | 특징 | 온프레미스 적합성 |
|---|---|---|
| **Flannel** | 단순, 오버헤드 낮음, 실험에 충분 | ✅ 추천 |
| Calico | NetworkPolicy 기능 강력, 복잡 | 과도함 |
| Cilium | eBPF 기반, 최신, 복잡 | 과도함 |

**→ 결정: Flannel 추천 (단순함 우선)**

---

### 1-6. k8s 버전

온프레미스와 EKS가 완전히 동일한 버전이어야 한다.  
EKS에서 지원하는 버전 중 최신 안정 버전으로 맞춘다.

```bash
# EKS 지원 버전 확인
aws eks describe-addon-versions --query 'addons[0].addonVersions[0].compatibilities[*].clusterVersion'
```

**→ 결정 필요: EKS 지원 최신 안정 버전으로 양쪽 동일하게**

---

### 1-7. 실험 동시 시작 방법

두 환경이 동시에 시나리오를 시작해야 공정한 비교가 된다.

**옵션 A: 구두 카운트다운 (Slack/화상통화 "3, 2, 1, 시작")**  
**옵션 B: Locust 스케줄 기능 (특정 시각에 자동 시작)**  
**옵션 C: GitHub Actions workflow dispatch (동시 트리거)**

**→ ✅ 결정: 옵션 B — Locust 스케줄 기능**  
`--run-time` + `--headless` + `--spawn-rate` 조합으로 특정 시각에 자동 시작.  
두 환경 동시 시작 시각을 스크립트에 고정해서 사람 개입 없이 동기화.

---

### 결정 사항 요약표

| # | 항목 | 결정 | 비고 |
|:---:|---|---|---|
| 1 | **계정 구조** | ✅ 단일 계정 우선 → 크레딧 부족 시 2계정 전환 | 전환해도 scrape 설정 변경 최소화 |
| 2 | **VPC 구조** | ✅ VPC 2개 (온프레미스+공통 \| EKS), Public 서브넷만 | — |
| 3 | **Prometheus** | ✅ 계정 무관 단일 Prometheus (VPC-A t3.small) | 2계정 전환 시 사설IP→공인IP 변경만 |
| 4 | **Locust 배치** | ✅ 계정 무관 2대 (Locust-A: 온프레미스 / Locust-B: EKS 전용) | 스케줄 기능으로 동시 시작 |
| 5 | **ECR** | ✅ 단일 계정: 공유 / 2계정: 각자 push | — |
| 6 | **실험 동시 시작** | ✅ Locust `--headless` 스케줄 기능 | 수동 카운트다운 불필요 |
| 7 | **CNI** | ⬜ 고민 중 (Flannel 추천) | — |
| 8 | **k8s 버전** | ⬜ 결정 필요 (EKS 지원 최신 안정) | 양쪽 동일 버전 필수 |

---

## 2. 온프레미스 k8s — 전체 작업

### 2-1. AWS 기반 인프라 — VPC-A (온프레미스 + 공통)

> 단일 계정 기준. 2계정 전환 시 이 VPC를 온프레미스 계정으로 이동.

- [ ] **VPC-A** 생성 (CIDR: 10.0.0.0/16, ap-northeast-2)
- [ ] Public 서브넷 생성 (10.0.1.0/24, ap-northeast-2a)
- [ ] Internet Gateway 생성 + VPC-A 연결
- [ ] 라우팅 테이블: 0.0.0.0/0 → Internet Gateway
- [ ] Security Group 생성 (c8i.2xlarge, PostgreSQL, Redis, Monitoring, Locust 각각)
- [ ] c8i.2xlarge EC2 생성 (Ubuntu 22.04, Public 서브넷)
- [ ] PostgreSQL EC2 생성 (t3.medium, Ubuntu 22.04, Public 서브넷)
- [ ] Redis EC2 생성 (t3.micro, Ubuntu 22.04, Public 서브넷)
- [ ] Monitoring EC2 생성 (t3.small, Ubuntu 22.04, Public 서브넷)
- [ ] Locust EC2 생성 (t3.medium, Ubuntu 22.04, Public 서브넷)
- [ ] **EIP 할당**: c8i.2xlarge, Monitoring EC2 (SG 화이트리스트에 등록되므로 IP 고정 필수)

---

### 2-2. KVM 가상머신 구성 (c8i.2xlarge 내부)

- [ ] c8i.2xlarge에 KVM 설치
  ```bash
  sudo apt install -y qemu-kvm libvirt-daemon-system virtinst bridge-utils
  sudo systemctl enable --now libvirtd
  ```
- [ ] KVM 브릿지 네트워크 설정 (virbr0: 192.168.122.0/24)
- [ ] VM1 생성: k8s 마스터 (2vCPU, 4GB RAM, Ubuntu 22.04)
- [ ] VM2 생성: 워커노드 1 (2vCPU, 4GB RAM, Ubuntu 22.04)
- [ ] VM3 생성: 워커노드 2 (2vCPU, 4GB RAM, Ubuntu 22.04)
- [ ] VM 간 SSH 키 복사 (kubeadm join용)
- [ ] VM IP 고정 설정 (DHCP → static)
  ```
  VM1(마스터):   192.168.122.2
  VM2(워커1):    192.168.122.3
  VM3(워커2):    192.168.122.4
  ```

---

### 2-3. k8s 클러스터 구성

**모든 VM에서 공통 작업:**
- [ ] containerd 설치
- [ ] kubeadm / kubelet / kubectl 설치 (EKS와 동일 버전)
- [ ] swap 비활성화 (`swapoff -a`, `/etc/fstab` 수정)
- [ ] br_netfilter 모듈 로드 + net.bridge.bridge-nf-call-iptables=1

**마스터 VM(VM1)에서:**
- [ ] `kubeadm init --pod-network-cidr=10.244.0.0/16` (Flannel 기본 CIDR)
- [ ] kube-config 저장 (`~/.kube/config`)
- [ ] join 명령어 메모

**워커노드(VM2, VM3)에서:**
- [ ] `kubeadm join <master-ip>:6443 --token ...`

**마스터에서 CNI 설치:**
- [ ] Flannel 설치
  ```bash
  kubectl apply -f https://raw.githubusercontent.com/flannel-io/flannel/master/Documentation/kube-flannel.yml
  ```
- [ ] 노드 Ready 상태 확인: `kubectl get nodes`

---

### 2-4. k8s 부가 컴포넌트 설치

- [ ] **Metrics Server** (HPA 전제 조건)
  ```bash
  kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
  ```
  설치 후 확인: `kubectl top nodes`  
  > KVM 환경에서 TLS 이슈 발생 시 `--kubelet-insecure-tls` 플래그 추가 필요

- [ ] **MetalLB** (LoadBalancer 타입 서비스 IP 할당)
  ```bash
  kubectl apply -f https://raw.githubusercontent.com/metallb/metallb/main/config/manifests/metallb-native.yaml
  ```
  IPAddressPool + L2Advertisement 설정 (192.168.122.200-210)

- [ ] **Nginx Ingress Controller**
  ```bash
  kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.x.x/deploy/static/provider/baremetal/deploy.yaml
  ```

- [ ] **kube-state-metrics** (k8s 상태 메트릭 수집)
  ```bash
  kubectl apply -f https://github.com/kubernetes/kube-state-metrics/tree/main/examples/standard
  ```

- [ ] **Node Exporter** (DaemonSet으로 각 노드 메트릭 수집)
  ```bash
  # Helm 또는 직접 DaemonSet yaml 적용
  ```

---

### 2-5. EC2 호스트 리버스 프록시 설정

- [ ] c8i.2xlarge 호스트에 Nginx 설치 (`sudo apt install nginx`)
- [ ] MetalLB 가상 IP로 프록시 설정
  ```nginx
  server {
      listen 80;
      location / {
          proxy_pass http://192.168.122.200;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
      }
  }
  ```
- [ ] Nginx 재시작 및 80번 포트 동작 확인

---

### 2-6. ECR 인증 설정

- [ ] AWS CLI 설치 (VM들 또는 호스트에서)
- [ ] ECR Secret 생성
  ```bash
  aws ecr get-login-password --region ap-northeast-2 \
    | kubectl create secret docker-registry ecr-secret \
        --docker-server=<ACCOUNT_ID>.dkr.ecr.ap-northeast-2.amazonaws.com \
        --docker-username=AWS \
        --docker-password=$(cat -)
  ```
- [ ] ECR 토큰 갱신 CronJob 배포 (11시간 주기)
- [ ] CronJob용 ServiceAccount + RBAC 생성

---

### 2-7. 노드 라벨링 및 실험 설정

- [ ] 워커노드 1 라벨: `kubectl label node <worker-1> experiment-role=traffic-node`
- [ ] 워커노드 2 라벨: `kubectl label node <worker-2> experiment-role=order-node`
- [ ] NetworkPolicy 적용 (default-deny + 서비스 간 허용 규칙)

---

### 2-8. PostgreSQL 설정 (t3.medium EC2)

- [ ] PostgreSQL 16 설치
  ```bash
  sudo apt install postgresql-16
  ```
- [ ] `postgresql.conf` 설정
  - `listen_addresses = '*'`
  - `max_connections = 200`
  - `shared_buffers = 256MB`
- [ ] `pg_hba.conf` 설정 (k8s 클러스터 IP 대역 허용)
  ```
  host shoply shoply 192.168.122.0/24 md5   # KVM 브릿지 대역
  host shoply shoply 10.0.0.0/16 md5         # VPC 대역
  ```
- [ ] DB 및 유저 생성
  ```sql
  CREATE DATABASE shoply;
  CREATE USER shoply WITH PASSWORD 'shoply1234';
  GRANT ALL PRIVILEGES ON DATABASE shoply TO shoply;
  ```
- [ ] schema.sql 적용
- [ ] seed.sql 적용 (약 44,800줄, 시간 소요 예상)
- [ ] PostgreSQL Exporter 설치 및 설정

---

### 2-9. Redis 설정 (t3.micro EC2)

- [ ] Redis 7 설치: `sudo apt install redis-server`
- [ ] `redis.conf` 설정
  - `bind 0.0.0.0` (외부 접근 허용)
  - `requirepass <password>` (선택)
  - `maxmemory 512mb`
  - `maxmemory-policy allkeys-lru`
- [ ] Redis 재시작: `sudo systemctl restart redis`
- [ ] Redis Exporter 설치 및 설정

---

## 3. EKS — 전체 작업

### 3-1. AWS 기반 인프라 — VPC-B (EKS)

> 단일 계정 기준. 2계정 전환 시 이 VPC를 EKS 계정으로 이동.

- [ ] **VPC-B** 생성 (CIDR: 10.1.0.0/16, ap-northeast-2)
- [ ] Public 서브넷 2개 생성 (EKS 최소 요구: 2개 AZ)
  - 10.1.1.0/24, ap-northeast-2a
  - 10.1.2.0/24, ap-northeast-2b
- [ ] Internet Gateway 생성 + VPC-B 연결
- [ ] 라우팅 테이블: 0.0.0.0/0 → Internet Gateway (양쪽 서브넷 모두)
- [ ] Security Group 생성 (ALB, Worker Node, RDS, Redis 각각)
- [ ] EKS 클러스터용 IAM Role 생성 (eksctl 사용 시 자동 생성)

---

### 3-2. EKS 클러스터 생성

- [ ] eksctl 또는 콘솔로 클러스터 생성
  ```bash
  eksctl create cluster \
    --name shoply-eks \
    --version 1.XX \                 # 온프레미스와 동일 버전
    --region ap-northeast-2 \
    --nodegroup-name initial-nodes \
    --node-type t3.medium \
    --nodes 2 \
    --nodes-min 2 \
    --nodes-max 10 \                 # Karpenter 추가 후 조정
    --managed
  ```
- [ ] kube-config 설정: `aws eks update-kubeconfig --name shoply-eks --region ap-northeast-2`
- [ ] 노드 Ready 확인: `kubectl get nodes`

---

### 3-3. EKS 부가 컴포넌트 설치

- [ ] **AWS Load Balancer Controller** (ALB 자동 생성)
  - IRSA 설정 (LB Controller용 IAM Role + ServiceAccount)
  - Helm으로 설치
  ```bash
  helm install aws-load-balancer-controller eks/aws-load-balancer-controller \
    -n kube-system \
    --set clusterName=shoply-eks \
    --set serviceAccount.create=false \
    --set serviceAccount.name=aws-load-balancer-controller
  ```

- [ ] **Nginx Ingress Controller** (온프레미스와 동일한 yaml 사용)
  ```bash
  kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/.../deploy/static/provider/aws/deploy.yaml
  ```
  > AWS provider 버전은 Service에 NLB annotation이 자동 포함됨

- [ ] **Karpenter** (노드 자동 확장)
  - Karpenter용 IAM Role + IRSA 설정
  - NodePool 설정 (t3.medium 우선, 최대 스펙 제한)
  - EC2NodeClass 설정 (AMI, 서브넷, SG)
  ```yaml
  # NodePool: 실험에서 Karpenter가 추가할 노드 스펙 제한
  apiVersion: karpenter.sh/v1
  kind: NodePool
  spec:
    template:
      spec:
        requirements:
        - key: node.kubernetes.io/instance-type
          operator: In
          values: ["t3.medium", "t3.large"]
    limits:
      cpu: 32           # 최대 CPU 제한 (비용 보호)
  ```

- [ ] **Metrics Server** 설치 확인 (EKS에서 자동이지만 확인)
  ```bash
  kubectl top nodes
  ```
  없으면 직접 설치

- [ ] **kube-state-metrics** 설치 (온프레미스와 동일)
- [ ] **Node Exporter** DaemonSet 설치 (온프레미스와 동일)

---

### 3-4. 노드 라벨링 및 실험 설정

- [ ] 워커노드 1 라벨: `experiment-role=traffic-node`
- [ ] 워커노드 2 라벨: `experiment-role=order-node`
- [ ] NetworkPolicy 적용 (온프레미스와 동일한 yaml)

> Karpenter가 추가하는 노드에는 라벨 없음 → HPA 파드 자유 배치 (의도)

---

### 3-5. RDS 설정

- [ ] RDS 서브넷 그룹 생성 (2개 AZ 서브넷)
- [ ] RDS db.t3.medium 생성
  - Engine: PostgreSQL 16 (온프레미스와 동일 버전)
  - DB Name: shoply
  - Username: shoply
  - Password: shoply1234
  - Security Group: RDS SG 연결
  - 자동 백업: 활성화 (실험 전 복원용)
- [ ] RDS 파라미터 그룹
  - `max_connections`: 200 (온프레미스와 동일하게 맞춤)
  - `shared_buffers`: RDS 자동 관리 (직접 수정 불가)
- [ ] RDS 엔드포인트 확인 후 k8s Secret으로 등록
- [ ] schema.sql 적용 (psql 클라이언트로 접속)
- [ ] seed.sql 적용

---

### 3-6. Redis 설정

- [ ] Redis EC2 t3.micro 생성 (온프레미스와 동일하게 직접 설치)
- [ ] Redis 설정 (온프레미스와 동일한 conf)
- [ ] Redis Exporter 설치

---

## 4. 공통 인프라 — 전체 작업

### 4-1. Monitoring EC2 (VPC-A, t3.small, EIP 필수)

> EIP 필수 이유: EKS Worker Node SG 인바운드에 이 IP를 화이트리스트 등록.  
> 재시작 후 IP가 바뀌면 SG를 다시 수정해야 함 → EIP로 고정.

**Prometheus 설치 및 설정:**
- [ ] Prometheus 설치 (바이너리 권장)
  ```bash
  wget https://github.com/prometheus/prometheus/releases/download/v2.x.x/prometheus-2.x.x.linux-amd64.tar.gz
  tar xvf prometheus-*.tar.gz && sudo mv prometheus /usr/local/bin/
  ```
- [ ] `prometheus.yml` 작성
  ```yaml
  global:
    scrape_interval: 15s
  
  scrape_configs:
  
  # ── 온프레미스 ──────────────────────────────────────
  - job_name: 'kube-state-metrics-onprem'
    static_configs:
    - targets: ['<c8i.2xlarge-사설IP>:<NodePort>']  # 단일계정: 사설IP
    relabel_configs:
    - target_label: env
      replacement: onprem
  
  - job_name: 'app-onprem'            # prom-client (각 서비스 /metrics)
    static_configs:
    - targets:
      - '<c8i.2xlarge-사설IP>:<gateway-NodePort>'
      - '<c8i.2xlarge-사설IP>:<product-NodePort>'
      - '<c8i.2xlarge-사설IP>:<inventory-NodePort>'
      - '<c8i.2xlarge-사설IP>:<order-NodePort>'
      - '<c8i.2xlarge-사설IP>:<payment-NodePort>'
      - '<c8i.2xlarge-사설IP>:<user-NodePort>'
    relabel_configs:
    - target_label: env
      replacement: onprem
  
  - job_name: 'postgresql-onprem'
    static_configs:
    - targets: ['<PostgreSQL-사설IP>:9187']
    relabel_configs:
    - target_label: env
      replacement: onprem
  
  - job_name: 'redis-onprem'
    static_configs:
    - targets: ['<Redis-사설IP>:9121']
    relabel_configs:
    - target_label: env
      replacement: onprem
  
  - job_name: 'node-onprem'           # VM Node Exporter
    static_configs:
    - targets:
      - '192.168.122.2:9100'          # VM1 마스터
      - '192.168.122.3:9100'          # VM2 워커1
      - '192.168.122.4:9100'          # VM3 워커2
    relabel_configs:
    - target_label: env
      replacement: onprem
  
  # ── EKS ─────────────────────────────────────────────
  - job_name: 'kube-state-metrics-eks'
    static_configs:
    - targets: ['<EKS-worker1-공인IP>:<NodePort>']  # 항상 공인IP (cross-VPC)
    relabel_configs:
    - target_label: env
      replacement: eks
  
  - job_name: 'app-eks'
    static_configs:
    - targets:
      - '<EKS-worker1-공인IP>:<gateway-NodePort>'
      - '<EKS-worker1-공인IP>:<product-NodePort>'
      - '<EKS-worker1-공인IP>:<inventory-NodePort>'
      - '<EKS-worker2-공인IP>:<order-NodePort>'
      - '<EKS-worker2-공인IP>:<payment-NodePort>'
      - '<EKS-worker2-공인IP>:<user-NodePort>'
    relabel_configs:
    - target_label: env
      replacement: eks
  
  - job_name: 'postgresql-eks'        # EKS 내부 k8s Pod → NodePort
    static_configs:
    - targets: ['<EKS-worker-공인IP>:<pg-exporter-NodePort>']
    relabel_configs:
    - target_label: env
      replacement: eks
  
  - job_name: 'redis-eks'
    static_configs:
    - targets: ['<EKS-Redis-공인IP>:9121']
    relabel_configs:
    - target_label: env
      replacement: eks
  
  - job_name: 'node-eks'
    static_configs:
    - targets:
      - '<EKS-worker1-공인IP>:9100'
      - '<EKS-worker2-공인IP>:9100'
    relabel_configs:
    - target_label: env
      replacement: eks
  ```
- [ ] Prometheus systemd 서비스 등록 + 자동 시작

**Grafana 설치 및 설정:**
- [ ] Grafana 설치
  ```bash
  sudo apt install -y grafana
  sudo systemctl enable --now grafana-server
  ```
- [ ] 포트 3001로 변경 (`/etc/grafana/grafana.ini` → `http_port = 3001`)
- [ ] DataSource 등록: Prometheus (http://localhost:9090)
- [ ] 템플릿 대시보드 import:
  - Node Exporter Full (ID: **1860**)
  - Kubernetes Cluster (ID: **315**)
  - PostgreSQL (ID: **9628**)
  - Redis (ID: **11835**)
- [ ] 직접 제작 패널 8개 구성 (온프레미스|EKS 나란히):
  - #1 TPS, #2 P95 Latency, #3 Error Rate
  - #4 Pod Count, #5 Node Count, #6 Pending Pod, #7 HPA current vs desired
  - #8 API 엔드포인트별 응답시간

**2계정 전환 시:**  
온프레미스 scrape 대상을 사설IP → 공인IP로 일괄 변경 후 Prometheus reload.

---

### 4-2. Locust-A EC2 (VPC-A, t3.medium, EIP) — 온프레미스 전용

- [ ] EC2 생성 + EIP 할당
- [ ] Python + Locust 설치
  ```bash
  sudo apt install -y python3-pip && pip install locust
  ```
- [ ] GitHub 공통 레포에서 locustfile.py pull
- [ ] 시나리오별 실행 명령어 확인 (아래 참고)
- [ ] 온프레미스 LB 접근 테스트: `curl http://<c8i.2xlarge-사설IP>/api/products`

---

### 4-3. Locust-B EC2 (VPC-A, t3.medium, EIP) — EKS 전용

- [ ] EC2 생성 + EIP 할당
- [ ] **Locust-B EIP → EKS ALB SG 인바운드에 등록** (이 순서 중요)
- [ ] Python + Locust 설치 (Locust-A와 동일)
- [ ] GitHub 공통 레포에서 locustfile.py pull (동일 스크립트)
- [ ] EKS ALB 접근 테스트: `curl http://<ALB-DNS>/api/products`

---

### 4-4. Locust 스크립트 (GitHub 공통 레포 관리)

```python
# locustfile.py
from locust import HttpUser, task, between, constant_pacing

class ShopUser(HttpUser):
    wait_time = constant_pacing(1)   # RPS 고정용

    def on_start(self):
        # 로그인 → JWT 획득
        res = self.client.post("/api/auth/login",
                               json={"email": "test@test.com", "password": "test1234"})
        self.token = res.json().get("token", "")
        self.headers = {"Authorization": f"Bearer {self.token}"}

    # 시나리오별 비율은 @task 가중치로 조정
    @task(5)
    def get_products(self):
        self.client.get("/api/products", headers=self.headers)

    @task(3)
    def create_order(self):
        self.client.post("/api/orders",
                         json={"product_id": "...", "size": 270, "quantity": 1},
                         headers=self.headers)

    @task(2)
    def create_payment(self):
        self.client.post("/api/payments",
                         json={"order_id": "..."},
                         headers=self.headers)
```

**시나리오별 실행 명령어:**
```bash
# 시나리오 1 — 200 RPS, 20분 (두 EC2에서 동시 실행)
locust -f locustfile.py --host http://<타겟-IP> \
  --headless --users 200 --spawn-rate 200 --run-time 20m

# 시나리오 2 — 100 RPS 워밍업 5분 후 1000 RPS로 급증
# (CustomShape 클래스로 단계별 RPS 구현)

# 시나리오 3 — 500 RPS 유지 (워커노드 종료는 별도로 수동/kubectl)
locust -f locustfile.py --host http://<타겟-IP> \
  --headless --users 500 --spawn-rate 500 --run-time 20m
```

- [ ] 시나리오 2 급증 구현 (LoadTestShape 클래스 — 100→1000 RPS 단계별)
- [ ] 두 EC2 동시 시작 확인 (SSH 2창 열고 동시 실행 or tmux)

---

## 5. 앱·서비스 배포 준비

### 5-1. 미완성 서비스 구현

- [ ] **Payment Service 구현** (현재 미구현, 포트 4004)
  - POST /payments — Mock 결제 (95% 성공률)
  - 성공 → Inventory `/deduct` 호출 → Order `/status` 업데이트
  - 실패 → Inventory `/release` 호출 → Order 상태 FAILED
  - GET /stats — 결제 성공/실패 건수 집계
  - prom-client 메트릭 포함

### 5-2. k8s 매니페스트 작성

각 서비스에 대해 아래 파일 작성 (`k8s/` 디렉토리):

- [ ] Deployment (nodeAffinity + resources.requests 필수)
- [ ] Service (ClusterIP — 내부 통신용)
- [ ] Service (NodePort — Prometheus scrape용, 포트 9464)
- [ ] HPA (CPU 60% 기준, min 1, max 5)
- [ ] ConfigMap (DB 호스트, Redis 호스트 등 환경별 값)
- [ ] Secret (DB 패스워드, JWT 시크릿)
- [ ] Ingress (Nginx Ingress 규칙)
- [ ] NetworkPolicy

**환경별 값 분리 전략:**
```
k8s/
├── base/           # 공통 manifest (Deployment 구조, Service)
└── overlays/
    ├── onprem/     # PostgreSQL/Redis 호스트 주소 (온프레미스용)
    └── eks/        # RDS/Redis 호스트 주소 (EKS용)
```
> kustomize 사용 권장 — 동일 yaml 구조에서 환경별 값만 오버라이드

### 5-3. Docker 이미지 빌드 확인

- [ ] 각 서비스 Dockerfile 점검
- [ ] 로컬에서 빌드 테스트
- [ ] 이미지 태그 전략 확정 (예: `git SHA` 또는 `v1.0.0`)

---

## 6. CI/CD 구성

- [ ] GitHub Actions workflow 작성
  ```yaml
  on:
    push:
      branches: [main]
  jobs:
    build-and-push:
      steps:
      - Docker 빌드
      - 온프레미스 팀 계정 ECR push
      - EKS 팀 계정 ECR push (동일 이미지)
    
    deploy-onprem:
      needs: build-and-push
      steps:
      - self-hosted runner (온프레미스 계정 EC2에 runner 설치)
      - kubectl apply (온프레미스 k8s)
    
    deploy-eks:
      needs: build-and-push
      steps:
      - aws eks update-kubeconfig
      - kubectl apply (EKS)
  ```
- [ ] GitHub Actions self-hosted runner 설치 (온프레미스 계정 EC2에)
- [ ] AWS IAM credentials 설정 (GitHub Secrets에 양쪽 계정 credentials)
- [ ] Trivy 스캔 step 유지 (기존 설정됨)
- [ ] 배포 후 Health Check step 추가

---

## 7. 실험 진행 체크리스트

### 실험 전 (매 시나리오 전)

- [ ] 양쪽 DB 초기화 (seed.sql 재적용 또는 RDS snapshot 복원)
- [ ] 양쪽 Redis FLUSH (`redis-cli FLUSHALL`)
- [ ] 양쪽 파드 정상 상태 확인 (`kubectl get pods -A`)
- [ ] Prometheus 수집 확인 (Grafana에서 데이터 들어오는지)
- [ ] Locust 연결 테스트 (LB에 curl 응답 확인)
- [ ] 모든 NodePort scrape 연결 확인
- [ ] Grafana 대시보드 정상 표시 확인

### 실험 중

- [ ] 시나리오 1: 200 RPS, 20분 — 양쪽 동시 시작
- [ ] 시나리오 2: 100→1000 RPS 급증, 20분 — 정확한 급증 시각 기록
- [ ] 시나리오 3: 500 RPS 중 워커노드 2 강제 종료 (`kubectl drain` 또는 EC2 stop)
- [ ] 각 시나리오에서 Grafana 스냅샷 저장

### 실험 후

- [ ] Grafana 스냅샷 or 패널 이미지 저장
- [ ] Prometheus 데이터 백업 (발표용)
- [ ] 결제 실패 건수 기록 (`payments` 테이블 집계)
- [ ] MTTR 기록 (워커노드 장애 시작 → 서비스 정상 시각)

---

## 8. 현재 상태 요약

### 완료

| 항목 | 상태 |
|---|---|
| User Service | ✅ |
| Product Service | ✅ |
| Inventory Service | ✅ |
| Order Service | ✅ |
| Frontend | ✅ |
| API Gateway | ✅ |
| PostgreSQL 스키마 + 시드 | ✅ |
| 로컬 docker-compose 환경 | ✅ |
| GitHub Actions 기본 CI (빌드 + Trivy) | ✅ |

### 미완료 (해야 할 것)

| 항목 | 우선순위 | 담당 |
|---|---|---|
| **Payment Service 구현** | 🔴 최우선 | — |
| 결정 사항 1-7 합의 | 🔴 최우선 | 팀 전체 |
| k8s 매니페스트 작성 (전 서비스) | 🔴 높음 | — |
| 온프레미스 k8s 클러스터 구성 (2-1~2-9) | 🔴 높음 | — |
| EKS 클러스터 구성 (3-1~3-6) | 🔴 높음 | — |
| Locust 스크립트 작성 | 🟡 중간 | — |
| Grafana 대시보드 구성 | 🟡 중간 | — |
| CI/CD 완성 (자동 배포까지) | 🟡 중간 | — |
| Prometheus scrape 전체 설정 | 🟡 중간 | — |
| 아키텍처 흐름도 작성 | 🟢 낮음 (마지막) | — |

---

## 작업 진행 권장 순서

```
Phase 1 — 결정 (팀 합의, 1~2일)
  → 섹션 1의 결정 사항 7개 모두 합의

Phase 2 — 앱 완성 (1~2일)
  → Payment Service 구현
  → k8s 매니페스트 작성

Phase 3 — 인프라 구성 (3~5일)
  → 온프레미스 k8s 클러스터 구성 (2-1~2-9)
  → EKS 클러스터 구성 (3-1~3-6)
  → 공통 모니터링 구성 (4-1)
  ← 양쪽 병렬 진행 권장

Phase 4 — 통합 및 배포 (1~2일)
  → CI/CD 완성
  → 서비스 배포 및 E2E 테스트

Phase 5 — 실험 리허설 (1일)
  → Locust 연결 테스트
  → 시나리오 1 리허설 (실제 실험 전 검증)

Phase 6 — 실험 본 진행
  → 시나리오 1 → 2 → 3 순서

Phase 7 — 마무리
  → 아키텍처 흐름도 작성
  → 발표 자료 구성
```
