# 모니터링 구성 가이드

> 우리는 웹을 만드는 팀이 아니라 **온프레미스 vs EKS 운영 특성을 비교하는 팀**이다.  
> 수집하는 모든 메트릭은 "비교 결과 증명" 또는 "원인 설명" 또는 "실험 공정성 검증" 중 하나의 이유가 있어야 한다.

---

## 목차

1. [포트 및 수집 엔드포인트 확정](#1-포트-및-수집-엔드포인트-확정)
2. [수집 구조](#2-수집-구조)
3. [왜 이걸 수집하는가 — 수집 목적별 분류](#3-왜-이걸-수집하는가--수집-목적별-분류)
4. [실제 메트릭 이름 목록](#4-실제-메트릭-이름-목록)
5. [Prometheus scrape 설정](#5-prometheus-scrape-설정)
6. [Grafana 템플릿 패널](#6-grafana-템플릿-패널)
7. [직접 제작 패널 — PromQL 포함](#7-직접-제작-패널--promql-포함)
8. [최종 대시보드 레이아웃](#8-최종-대시보드-레이아웃)
9. [시나리오별 주목 패널](#9-시나리오별-주목-패널)

---

## 1. 포트 및 수집 엔드포인트 확정

> 코드 직접 확인 기준. 설계에서 가정한 값이 아니라 실제 동작하는 값.

### 서비스 포트

| 서비스 | 컨테이너 포트 | /metrics 위치 | 비고 |
|---|:---:|---|---|
| Frontend (Nginx) | **80** | 없음 | docker-compose `3000:80` — 호스트 3000이 컨테이너 80에 매핑 |
| API Gateway | **4000** | `4000/metrics` | 앱과 같은 포트 |
| Product Service | **4001** | `4001/metrics` | |
| Inventory Service | **4002** | `4002/metrics` | |
| Order Service | **4003** | `4003/metrics` | |
| Payment Service | **4004** | `4004/metrics` | |
| User Service | **4005** | `4005/metrics` | |

### 인프라 Exporter 포트

| Exporter | 배포 방식 | 실행 위치 | 포트 |
|---|---|---|:---:|
| **kube-state-metrics** | k8s **Deployment** (1개) | 워커노드 중 하나에 스케줄 → k8s API Server에 쿼리 | **8080** → NodePort |
| **node_exporter** | k8s **DaemonSet** (모든 노드에 1개씩) | 마스터 + 워커 전체 | **9100** (노드IP 직접) |
| postgresql_exporter | EC2 직접 설치 (온프레미스) | PostgreSQL EC2 | **9187** |
| postgresql_exporter | k8s **Pod** (EKS) | EKS RDS는 VPC 내부에서만 접근 → Pod로 배포 후 NodePort | NodePort |
| redis_exporter | EC2 직접 설치 | Redis EC2 (양쪽) | **9121** |

> **kube-state-metrics vs node_exporter 차이:**
> - kube-state-metrics: 클러스터 전체에 **1개**만 뜸. k8s API Server에 물어봐서 파드·노드·HPA 같은 **k8s 오브젝트 상태**를 수집
> - node_exporter: **모든 노드에 각 1개**씩 뜸. 해당 노드의 **OS 수준** (CPU, Memory, 디스크)을 수집

### k8s에서 Prometheus scrape 방식

```
Prometheus (VPC-A t3.small)
  │
  ├── 앱 서비스 메트릭 (prom-client, /metrics)
  │     온프레미스: c8i.2xlarge 사설IP : NodePort
  │     EKS:       Worker Node 공인IP  : NodePort
  │
  ├── kube-state-metrics (Deployment Pod 1개)
  │     온프레미스: c8i.2xlarge 사설IP : NodePort  ← Pod가 어느 워커에 있든 NodePort로 접근
  │     EKS:       Worker Node 공인IP  : NodePort
  │
  ├── node_exporter (DaemonSet — 노드마다 1개)
  │     온프레미스: VM2 192.168.122.3:9100  ← 워커노드 1
  │                VM3 192.168.122.4:9100  ← 워커노드 2
  │                VM1 192.168.122.2:9100  ← 마스터 (참고용)
  │     EKS:       Worker Node1 공인IP:9100
  │                Worker Node2 공인IP:9100
  │
  ├── PostgreSQL Exporter
  │     온프레미스: PostgreSQL EC2 사설IP:9187
  │     EKS:       k8s Pod → Worker Node 공인IP:NodePort
  │
  └── Redis Exporter
        온프레미스: Redis EC2 사설IP:9121
        EKS:       Redis EC2 공인IP:9121
```

---

## 2. 수집 구조

```
온프레미스 k8s (VPC-A)                    EKS (VPC-B)
──────────────────────────────────        ──────────────────────────────────
  [k8s Pod — 앱 서비스]                     [k8s Pod — 앱 서비스]
    Gateway   :4000/metrics                   Gateway   :4000/metrics
    Product   :4001/metrics                   Product   :4001/metrics
    Inventory :4002/metrics                   Inventory :4002/metrics
    Order     :4003/metrics                   Order     :4003/metrics
    Payment   :4004/metrics                   Payment   :4004/metrics

  [k8s Pod — Deployment 1개]               [k8s Pod — Deployment 1개]
    kube-state-metrics :8080                  kube-state-metrics :8080
    └→ k8s API Server에 쿼리                  └→ EKS API Server에 쿼리
       (파드/노드/HPA 상태 수집)                  (파드/노드/HPA 상태 수집)

  [DaemonSet — 노드마다 1개씩]              [DaemonSet — 노드마다 1개씩]
    VM2 (워커1) node_exporter :9100           Worker Node1 node_exporter :9100
    VM3 (워커2) node_exporter :9100           Worker Node2 node_exporter :9100
    VM1 (마스터) node_exporter :9100
    └→ 각 노드의 CPU/Memory/디스크              └→ 각 노드의 CPU/Memory/디스크

  [k8s 외부 EC2]                           [k8s 외부 / AWS 관리형]
    PostgreSQL EC2 :9187                      postgresql_exporter Pod :NodePort
    Redis EC2      :9121                      Redis EC2 (공인IP)      :9121
             │                                             │
             └──────────── scrape (15s) ──────────────────┘
                                    ↓
                       Prometheus (VPC-A t3.small)
                         env="onprem" | env="eks"  ← relabel로 구분
                                    ↓
                       Grafana :3001 (VPC-A t3.small)
                         온프레미스 | EKS 패널 나란히
```

---

## 3. 왜 이걸 수집하는가 — 수집 목적별 분류

### 목적 A: 비교 결과 증명 — 발표에서 보여줄 것

이 지표들이 없으면 실험 자체가 성립하지 않는다.

| 지표 | 수집 도구 | 왜 필요한가 |
|---|---|---|
| **Pending Pod 수** | kube-state-metrics | "온프레미스는 자원 부족으로 파드가 쌓인다" — 발표 핵심 한 방 |
| **Node Count** | kube-state-metrics | "EKS는 Karpenter가 자동으로 노드를 추가한다" — 자동화 증거 |
| **HPA desired vs current** | kube-state-metrics | "늘리고 싶은데 못 늘린다" — 자원 부족 상태 정량화 |
| **Error Rate (5xx)** | prom-client (Gateway) | "온프레미스 37%, EKS 0.3%" — 비즈니스 임팩트 |
| **MTTR** | Error Rate 그래프 + 타임스탬프 | "복구 10분 vs 45초" — 시나리오 3 핵심 |

### 목적 B: 원인 설명 — 왜 저런 결과가 나왔는지

"데이터가 말한다"는 발표 메시지를 뒷받침하는 근거.

| 지표 | 수집 도구 | 왜 필요한가 |
|---|---|---|
| **CPU 사용률 per node** | node_exporter | "워커노드 1이 CPU 100%에 도달했기 때문에 Pending 발생" |
| **DB Active Connection** | postgresql_exporter | "커넥션이 고갈되어 쿼리가 대기 중" |
| **DB Lock 대기** | postgresql_exporter | "SELECT FOR UPDATE 충돌로 재고 처리가 지연" |
| **TPS** | prom-client (Gateway) | "온프레미스는 처리량이 한계에서 꺾임" |
| **P95 Latency** | prom-client (Gateway) | "응답시간이 얼마나 튀는지 — 사용자 체감 지표" |
| **재고 충돌 횟수** | prom-client (Inventory) | "동시 주문이 몰릴 때 충돌 건수 직접 측정" |
| **결제 성공/실패 수** | prom-client (Payment) | "장애 중 실제로 몇 건 손실됐는지" |

### 목적 C: 실험 공정성 검증 — 조건이 동일했음을 증명

"같은 조건에서 비교했다"는 전제를 데이터로 증명.

| 지표 | 수집 도구 | 왜 필요한가 |
|---|---|---|
| **Redis 캐시 히트율** | redis_exporter | "양쪽 캐시 조건이 동일했음을 확인" |
| **Memory 사용률** | node_exporter | "노드 스펙이 동일했음을 확인" |

### 수집하지 않는 것 (이유 포함)

| 항목 | 제외 이유 |
|---|---|
| Frontend 메트릭 | 비교 대상 아님. 정적 파일 서빙은 두 환경에서 동일 |
| ALB 상세 메트릭 | Gateway prom-client Error Rate로 충분히 커버 |
| 서비스별 개별 TPS/Error Rate | Gateway가 전체를 대표. 엔드포인트별 응답시간으로 보완 |
| 디스크 I/O | 이 실험의 비교 변수가 아님 |

---

## 4. 실제 메트릭 이름 목록

> 코드에서 직접 확인한 값. PromQL 작성 시 이 이름을 사용해야 한다.

### prom-client — 각 서비스별 Histogram

모든 서비스는 Histogram 타입으로 HTTP 응답시간을 수집한다.  
Histogram에서 파생되는 suffix:
- `_bucket` → `histogram_quantile()`로 P95 계산
- `_count` → `rate()`로 TPS 계산
- `_sum` → 평균 응답시간 계산 (sum/count)

| 서비스 | 메트릭 이름 | 레이블 |
|---|---|---|
| API Gateway | `gateway_http_duration_seconds` | `method`, `route`, `status` |
| Product | `product_service_http_duration_seconds` | `method`, `route`, `status` |
| Inventory | `inventory_service_http_duration_seconds` | `method`, `route`, `status` |
| Inventory | `inventory_stock_conflicts_total` | (충돌 횟수 Counter) |
| Order | `order_service_http_duration_seconds` | `method`, `route`, `status` |
| Payment | `payment_service_http_duration_seconds` | `method`, `route`, `status` |
| Payment | `payment_processed_total` | (결제 처리 Counter) |
| User | `user_service_http_duration_seconds` | `method`, `route`, `status` |

### 환경 구분 레이블

Prometheus scrape 시 relabeling으로 `env` 레이블 자동 부여:
- 온프레미스에서 수집된 모든 메트릭: `env="onprem"`
- EKS에서 수집된 모든 메트릭: `env="eks"`

---

## 5. Prometheus scrape 설정

```yaml
# /etc/prometheus/prometheus.yml

global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:

  # ── 온프레미스 앱 메트릭 (Gateway가 핵심) ────────────────────
  - job_name: 'app-onprem'
    static_configs:
    - targets:
      - '<c8i.2xlarge-사설IP>:<gateway-NodePort>'     # Gateway :4000/metrics
      - '<c8i.2xlarge-사설IP>:<product-NodePort>'     # Product :4001/metrics
      - '<c8i.2xlarge-사설IP>:<inventory-NodePort>'   # Inventory :4002/metrics
      - '<c8i.2xlarge-사설IP>:<order-NodePort>'       # Order :4003/metrics
      - '<c8i.2xlarge-사설IP>:<payment-NodePort>'     # Payment :4004/metrics
    relabel_configs:
    - target_label: env
      replacement: onprem

  # ── EKS 앱 메트릭 ────────────────────────────────────────────
  - job_name: 'app-eks'
    static_configs:
    - targets:
      - '<EKS-worker1-공인IP>:<gateway-NodePort>'
      - '<EKS-worker1-공인IP>:<product-NodePort>'
      - '<EKS-worker1-공인IP>:<inventory-NodePort>'
      - '<EKS-worker2-공인IP>:<order-NodePort>'
      - '<EKS-worker2-공인IP>:<payment-NodePort>'
    relabel_configs:
    - target_label: env
      replacement: eks

  # ── 온프레미스 k8s 상태 ───────────────────────────────────────
  - job_name: 'kube-state-onprem'
    static_configs:
    - targets: ['<c8i.2xlarge-사설IP>:<kube-state-NodePort>']
    relabel_configs:
    - target_label: env
      replacement: onprem

  # ── EKS k8s 상태 ─────────────────────────────────────────────
  - job_name: 'kube-state-eks'
    static_configs:
    - targets: ['<EKS-worker-공인IP>:<kube-state-NodePort>']
    relabel_configs:
    - target_label: env
      replacement: eks

  # ── PostgreSQL Exporter (온프레미스) ─────────────────────────
  - job_name: 'postgres-onprem'
    static_configs:
    - targets: ['<PostgreSQL-EC2-사설IP>:9187']
    relabel_configs:
    - target_label: env
      replacement: onprem

  # ── PostgreSQL Exporter (EKS — k8s Pod NodePort) ─────────────
  - job_name: 'postgres-eks'
    static_configs:
    - targets: ['<EKS-worker-공인IP>:<pg-exporter-NodePort>']
    relabel_configs:
    - target_label: env
      replacement: eks

  # ── Redis Exporter ────────────────────────────────────────────
  - job_name: 'redis-onprem'
    static_configs:
    - targets: ['<Redis-EC2-사설IP>:9121']
    relabel_configs:
    - target_label: env
      replacement: onprem

  - job_name: 'redis-eks'
    static_configs:
    - targets: ['<EKS-Redis-공인IP>:9121']
    relabel_configs:
    - target_label: env
      replacement: eks

  # ── Node Exporter (온프레미스 VM 3대) ────────────────────────
  - job_name: 'node-onprem'
    static_configs:
    - targets:
      - '192.168.122.2:9100'   # VM1 마스터 (참고용)
      - '192.168.122.3:9100'   # VM2 워커노드 1 (traffic-node)
      - '192.168.122.4:9100'   # VM3 워커노드 2 (order-node)
    relabel_configs:
    - target_label: env
      replacement: onprem

  # ── Node Exporter (EKS 워커노드) ─────────────────────────────
  - job_name: 'node-eks'
    static_configs:
    - targets:
      - '<EKS-worker1-공인IP>:9100'
      - '<EKS-worker2-공인IP>:9100'
    relabel_configs:
    - target_label: env
      replacement: eks
```

---

## 6. Grafana 템플릿 패널

> Grafana 공식 템플릿에서 Import 후 필요한 패널만 최종 대시보드로 복사.

### 6-1. Node Exporter Full — ID: `1860`

가져올 패널:

| 패널명 | 목적 |
|---|---|
| CPU 사용률 | "워커노드 1 CPU 100% 도달 → Pending 발생" 증거 |
| Memory 사용률 | 양쪽 노드 스펙 동일했음 확인 (공정성 검증) |
| Network I/O | 네트워크 포화 여부 참고 |

설정: `job="node-onprem"` / `job="node-eks"` 변수 전환

### 6-2. Kubernetes Cluster Monitoring — ID: `315`

가져올 패널:

| 패널명 | 목적 |
|---|---|
| Pod Count | HPA 발동으로 파드가 늘어나는 흐름 |
| Node Count | EKS Karpenter 자동 노드 추가 순간 포착 |
| **Pending Pod** | **발표 임팩트 최강** — 온프레미스 쌓임 / EKS 0 유지 |

설정: `job="kube-state-onprem"` / `job="kube-state-eks"` 변수 전환

### 6-3. PostgreSQL Database — ID: `9628`

가져올 패널:

| 패널명 | 목적 |
|---|---|
| Active Connection | "커넥션 고갈 → 쿼리 대기" 원인 설명 |
| 쿼리 응답시간 | 느려지는 구간 특정 |
| Lock 대기 | `SELECT FOR UPDATE` / 낙관적 락 충돌 시각화 |

설정: `job="postgres-onprem"` / `job="postgres-eks"`, `datname="shoply"`

### 6-4. Redis Dashboard — ID: `11835`

가져올 패널:

| 패널명 | 목적 |
|---|---|
| 캐시 히트율 | 양쪽 캐시 조건 동일했음 확인 (공정성 검증) |
| Memory 사용량 | Redis 메모리 포화 여부 |

---

## 7. 직접 제작 패널 — PromQL 포함

> 양쪽 환경을 나란히 비교하거나 템플릿에 없는 것들은 직접 만든다.  
> 모든 PromQL은 코드에서 확인한 실제 메트릭 이름 기준.

---

### #1 TPS (초당 처리 요청 수)

**왜:** 트래픽 폭증 시 온프레미스 처리량이 한계에서 꺾이는 순간을 포착

Histogram의 `_count`를 `rate()`로 계산:

```promql
# 온프레미스
rate(gateway_http_duration_seconds_count{env="onprem"}[1m])

# EKS
rate(gateway_http_duration_seconds_count{env="eks"}[1m])
```

---

### #2 P95 Latency (95번째 백분위 응답시간)

**왜:** 폭증 시 응답시간이 얼마나 튀는지 — 사용자 체감 지표

```promql
# 온프레미스
histogram_quantile(0.95,
  rate(gateway_http_duration_seconds_bucket{env="onprem"}[1m])
)

# EKS
histogram_quantile(0.95,
  rate(gateway_http_duration_seconds_bucket{env="eks"}[1m])
)
```

---

### #3 Error Rate (5xx 비율)

**왜:** "온프레미스 37% vs EKS 0.3%" — 발표 임팩트 최강, 비즈니스 손실 직결

```promql
# 온프레미스
rate(gateway_http_duration_seconds_count{env="onprem", status=~"5.."}[1m])
/
rate(gateway_http_duration_seconds_count{env="onprem"}[1m])

# EKS
rate(gateway_http_duration_seconds_count{env="eks", status=~"5.."}[1m])
/
rate(gateway_http_duration_seconds_count{env="eks"}[1m])
```

단위: `percent (0.0~1.0)`, Grafana에서 `Unit → Percent (0.0-1.0)` 설정

---

### #4 Pod Count 타임라인

**왜:** HPA 발동으로 파드가 증가하는 흐름 확인

```promql
# 온프레미스 Running Pod 수
count(kube_pod_status_phase{phase="Running", env="onprem"})

# EKS Running Pod 수
count(kube_pod_status_phase{phase="Running", env="eks"})
```

---

### #5 Node Count 타임라인

**왜:** EKS Karpenter 자동 노드 추가 순간을 타임라인으로 포착

```promql
# 온프레미스 (2개 고정선)
count(kube_node_info{env="onprem"})

# EKS (트래픽 폭증 후 계단식 상승)
count(kube_node_info{env="eks"})
```

---

### #6 Pending Pod 타임라인 ★

**왜:** 이 패널 하나가 모든 것을 설명한다  
온프레미스 — 자원 부족으로 계속 쌓임 / EKS — Karpenter가 해소하여 0 유지

```promql
# 온프레미스 (쌓임)
count(kube_pod_status_phase{phase="Pending", env="onprem"})
or on() vector(0)

# EKS (0 유지)
count(kube_pod_status_phase{phase="Pending", env="eks"})
or on() vector(0)
```

> `or on() vector(0)`: `or vector(0)`는 Pending Pod가 0개일 때 레이블 없는 시리즈를 반환해 Grafana 범례가 깨질 수 있음.  
> `on()`으로 빈 매칭을 명시하면 0을 안전하게 반환하고 그래프가 끊기지 않음.

---

### #7 HPA desired vs current replicas

**왜:** "HPA는 5개 원하는데 2개밖에 없다" — 자원 부족 상태를 정량으로 보여줌

```promql
# 온프레미스 — desired (원하는 수)
kube_horizontalpodautoscaler_status_desired_replicas{env="onprem"}

# 온프레미스 — current (실제 수)
kube_horizontalpodautoscaler_status_current_replicas{env="onprem"}

# EKS도 동일 (env="eks")
```

읽는 법: desired > current 구간 = 노드 자원 부족으로 파드를 못 띄우는 상태

---

### #8 CPU 사용률 per node

**왜:** "워커노드 1 CPU가 100%에 도달했기 때문에 Pending이 발생했다" — 원인 설명

```promql
# 온프레미스 워커노드별 CPU 사용률
1 - avg by (instance) (
  rate(node_cpu_seconds_total{mode="idle", env="onprem"}[1m])
)

# EKS 워커노드별
1 - avg by (instance) (
  rate(node_cpu_seconds_total{mode="idle", env="eks"}[1m])
)
```

---

### #9 재고 충돌 횟수 (Inventory Service 전용)

**왜:** 동시 주문 폭증 시 `SELECT FOR UPDATE` 충돌이 실제로 몇 번 발생했는지 직접 측정

```promql
# 온프레미스
rate(inventory_stock_conflicts_total{env="onprem"}[1m])

# EKS
rate(inventory_stock_conflicts_total{env="eks"}[1m])
```

---

### #10 결제 처리 건수 (Payment Service 전용)

**왜:** 시나리오 3 장애 중 실제로 몇 건의 결제가 성공/실패했는지 직접 측정

코드에서 `paymentTotal.inc({ status: payStatus })`로 `status` 레이블이 있으므로 성공/실패 분리 필수.

```promql
# 온프레미스 — 성공 (PAID)
rate(payment_processed_total{status="PAID", env="onprem"}[1m])

# 온프레미스 — 실패 (FAILED)
rate(payment_processed_total{status="FAILED", env="onprem"}[1m])

# EKS — 성공
rate(payment_processed_total{status="PAID", env="eks"}[1m])

# EKS — 실패
rate(payment_processed_total{status="FAILED", env="eks"}[1m])
```

> Grafana 패널에서 성공(초록) / 실패(빨강) 두 선을 겹쳐 그리면  
> 장애 구간에서 "성공 급감 + 실패 급증"이 한눈에 보임.

---

### #11 API 엔드포인트별 P95 응답시간

**왜:** `/api/products`와 `/api/orders` 중 어디서 병목이 먼저 발생하는지 특정

```promql
# 온프레미스 — 엔드포인트별 분리
histogram_quantile(0.95,
  sum by (route, le) (
    rate(gateway_http_duration_seconds_bucket{
      env="onprem",
      route=~"/api/(products|inventory|orders|payments).*"
    }[1m])
  )
)
```

확인 포인트: 시나리오 2에서 `/api/inventory`(재고)가 먼저 튀는지, `/api/products`(조회)가 먼저인지

---

## 8. 최종 대시보드 레이아웃

```
┌──────────────────────────────────────────────────────────────────┐
│  Row 1 — 비교 결과 (온프레미스 | EKS 나란히)                     │
│  ┌──────────┬───────────────┬──────────────────────────────────┐  │
│  │ #1 TPS   │ #2 P95 Latency│ #3 Error Rate ★                 │  │
│  └──────────┴───────────────┴──────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────────┤
│  Row 2 — k8s 반응 (온프레미스 | EKS 나란히)                      │
│  ┌──────────┬───────────────┬──────────────────────────────────┐  │
│  │ #4 Pod   │ #5 Node Count │ #6 Pending Pod ★★               │  │
│  │ Count    │               │                                  │  │
│  └──────────┴───────────────┴──────────────────────────────────┘  │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │ #7 HPA desired vs current                                   │  │
│  └─────────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────────┤
│  Row 3 — 원인 설명 (인프라)                                       │
│  ┌──────────┬───────────────┬──────────────────────────────────┐  │
│  │ #8 CPU   │ DB Connection │ DB Lock 대기                     │  │
│  │ per node │ (템플릿 9628) │ (템플릿 9628)                    │  │
│  └──────────┴───────────────┴──────────────────────────────────┘  │
│  ┌──────────┬───────────────┬──────────────────────────────────┐  │
│  │ Redis    │ #9 재고 충돌  │ #10 결제 처리 건수               │  │
│  │ 히트율   │ 횟수          │                                  │  │
│  └──────────┴───────────────┴──────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────────┤
│  Row 4 — 원인 설명 (API 병목)                                     │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │ #11 API 엔드포인트별 P95 응답시간                            │  │
│  └─────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

### 패널 출처 요약

| 구분 | # | 패널명 | 수집 도구 | 목적 |
|---|:---:|---|---|---|
| 직접 제작 | 1 | TPS | prom-client (Gateway) | 비교 결과 |
| 직접 제작 | 2 | P95 Latency | prom-client (Gateway) | 비교 결과 |
| 직접 제작 | 3 | Error Rate ★ | prom-client (Gateway) | 비교 결과 |
| 템플릿 #315 | 4 | Pod Count | kube-state-metrics | 비교 결과 |
| 템플릿 #315 | 5 | Node Count | kube-state-metrics | 비교 결과 |
| 템플릿 #315 | 6 | **Pending Pod ★★** | kube-state-metrics | **비교 결과 핵심** |
| 직접 제작 | 7 | HPA desired vs current | kube-state-metrics | 비교 결과 |
| 직접 제작 | 8 | CPU per node | node_exporter | 원인 설명 |
| 템플릿 #9628 | — | DB Connection | postgresql_exporter | 원인 설명 |
| 템플릿 #9628 | — | DB Lock 대기 | postgresql_exporter | 원인 설명 |
| 템플릿 #11835 | — | Redis 히트율 | redis_exporter | 공정성 검증 |
| 직접 제작 | 9 | 재고 충돌 횟수 | prom-client (Inventory) | 원인 설명 |
| 직접 제작 | 10 | 결제 처리 건수 | prom-client (Payment) | 원인 설명 |
| 직접 제작 | 11 | API 엔드포인트별 응답시간 | prom-client (Gateway) | 원인 설명 |

---

## 9. 시나리오별 주목 패널

### 시나리오 1 — 일반 사용자 흐름 (200 RPS, 20분)

> 목적: 기준선 형성. 온프레미스도 정상 운영 가능함을 먼저 보여줌.

| 주목 패널 | 기대 상태 (양쪽 동일) | 발표 포인트 |
|---|---|---|
| #1 TPS | 200 수평 유지 | "둘 다 처리 가능합니다" |
| #3 Error Rate | 0% | "에러 없습니다" |
| #4 Pod Count | 변화 없음 | "HPA 발동 없습니다" |
| #6 Pending Pod | 0 | "자원 여유 있습니다" |
| #8 CPU per node | 30~50% | "여유 있습니다" |

> **메시지:** "트래픽이 예측 가능하고 안정적이면 온프레미스도 충분합니다"

---

### 시나리오 2 — 동시 예약/주문 폭주 (순간 급증, 20분)

> 목적: 확장성 차이. 갑자기 1000 RPS로 터질 때 HPA + Karpenter 유무의 결과.

| 주목 패널 | 온프레미스 | EKS | 발표 포인트 |
|---|---|---|---|
| #1 TPS | 한계에서 수평 꺾임 | 계속 증가 | 처리량 한계 도달 |
| #3 Error Rate | **30~40% 급등** | **0~1% 유지** | **핵심 비교 지표** |
| #5 Node Count | 2개 고정 | 2→4~5개 | Karpenter 발동 |
| **#6 Pending Pod** | **계속 쌓임** | **0 유지** | **발표 임팩트 최강** |
| #7 HPA desired vs current | 괴리 큼 | 일치 | "늘리고 싶은데 못 늘림" |
| #8 CPU per node | 워커노드1 100% | 분산됨 | Pending 원인 |

> **메시지:** "여기서 운영 방식의 차이가 드러납니다"

---

### 시나리오 3 — 점진적 부하 증가 (계단식, 20분)

> 목적: 한계점 탐색. "X RPS부터 온프레미스가 무너지기 시작합니다"

| 주목 패널 | 온프레미스 | EKS | 발표 포인트 |
|---|---|---|---|
| #8 CPU per node | 특정 RPS 구간서 급등 | 분산 유지 | "N RPS부터 CPU 임계 도달" |
| #4 Pod Count | HPA 발동 → 증가 시도 | HPA + 신규 노드로 증가 | HPA 반응 시점 |
| **#6 Pending Pod** | 특정 RPS에서 쌓이기 시작 | 0 유지 (노드 추가로 해소) | **한계 RPS 특정** |
| #3 Error Rate | 특정 RPS에서 발생 | 발생 안 함 | "N RPS = 온프레미스 한계" |
| #5 Node Count | 2개 고정 | 특정 시점에 증가 | EKS 자동 대응 타이밍 |
| #11 API 엔드포인트별 응답시간 | 특정 RPS서 특정 API 먼저 튐 | 안정 유지 | 병목 API 특정 |

> **메시지:** "성능 차이보다 중요한 것은, 한계에 도달했을 때 자동으로 대응할 수 있느냐입니다"

---

### 시나리오 4 — 장애 복구 (500 RPS 중 워커노드 2 종료, 20분)

> 목적: 운영 복구력. 장애 발생 후 MTTR과 비즈니스 손실 수치 비교.

| 주목 패널 | 온프레미스 | EKS | 발표 포인트 |
|---|---|---|---|
| #3 Error Rate | 장애 직후 급등 → **5~10분 후 회복** | 장애 직후 급등 → **30~60초 후 회복** | **MTTR 차이** |
| #5 Node Count | 2→1→2 (수동 복구) | 2→1→3 (Karpenter 자동) | 복구 방식 차이 |
| #6 Pending Pod | 장애 후 다수 쌓임 → 천천히 해소 | 잠깐 후 해소 | 복구 흐름 |
| **#10 결제 처리 건수** | **장애 중 급감 → 손실 N건** | **최소 손실** | **비즈니스 임팩트 정량화** |

> **메시지:** "복구 시간이 곧 비즈니스 손실입니다"

---

### 발표 핵심 화면

**시나리오 2 — 확장성 비교**
```
┌──────────────────────────┬──────────────────────────┐
│       온프레미스          │        AWS EKS            │
│                          │                          │
│  Error Rate:  37%  ↑↑↑  │  Error Rate:   0.3% ───  │
│  Pending Pod: 23개  ↑↑↑  │  Pending Pod:  0    ───  │
│  Node Count:  2개   ───  │  Node Count:   4개  ↑    │
└──────────────────────────┴──────────────────────────┘
           이 화면 하나로 설명이 필요 없다
```

**시나리오 3 — 한계점 비교**
```
┌──────────────────────────┬──────────────────────────┐
│       온프레미스          │        AWS EKS            │
│                          │                          │
│  500 RPS에서:            │  1000 RPS에서도:          │
│  Error Rate 상승 시작    │  Error Rate 0% 유지      │
│  Pending Pod 발생        │  Node 자동 추가           │
└──────────────────────────┴──────────────────────────┘
    "온프레미스 한계: 약 500 RPS"
```

**시나리오 4 — MTTR 비교**
```
┌──────────────────────────┬──────────────────────────┐
│       온프레미스          │        AWS EKS            │
│                          │                          │
│  MTTR: 약 8분            │  MTTR: 약 45초            │
│  결제 실패: N건          │  결제 실패: 최소화        │
└──────────────────────────┴──────────────────────────┘
    "8분 vs 45초 — 이 시간이 비즈니스 손실입니다"
```

---

---

## 10. 수집 대상 전체 요약표

> 우리 프로젝트에서 모니터링 대상이 되는 컴포넌트, 수집 도구, 포트, 수집 이유를 한눈에 정리한 표.  
> 포트는 **컨테이너 실제 포트** 기준 (docker-compose 호스트 포트 아님).

| 컴포넌트 | 수집 도구 | 포트 | 수집하는 것 | 수집 이유 |
|---|---|:---:|---|---|
| **Frontend** (Nginx + React) | 없음 | 80 | — | 정적 파일 서빙은 비교 대상 아님. 백엔드 지표로 대체 |
| **API Gateway** (Express.js) | prom-client | **4000**/metrics | 전체 API 총 요청 수(TPS), HTTP 5xx 에러율, P95 응답시간 | 모든 트래픽의 진입점 — 온프레미스 vs EKS 처리량·에러율 직접 비교 |
| **Product Service** | prom-client | **4001**/metrics | 서비스별 응답시간 | `/api/products` 병목 여부 확인 (Read 70% 담당) |
| **Inventory Service** | prom-client | **4002**/metrics | 서비스별 응답시간, **재고 충돌 횟수** | SELECT FOR UPDATE 충돌 건수 직접 측정 — 동시성 병목 원인 설명 |
| **Order Service** | prom-client | **4003**/metrics | 서비스별 응답시간 | 시나리오 3 장애 복구 흐름 추적 |
| **Payment Service** | prom-client | **4004**/metrics | 서비스별 응답시간, **결제 처리 건수** | 장애 중 실제 결제 손실 건수 측정 — 비즈니스 임팩트 정량화 |
| **kube-state-metrics** | kube-state-metrics (k8s **Deployment** 1개) | **8080** → NodePort | Pending Pod 수, Node 수, HPA desired vs current, Pod 수 | **실험 핵심 지표** — 클러스터에 1개만 뜨고 k8s API Server에 쿼리해서 오브젝트 상태를 수집 |
| **VM / Worker Node** | node_exporter (k8s **DaemonSet** — 노드마다 1개) | **9100** (노드IP 직접) | CPU 사용률 per node, Memory 사용률 | "워커노드 1 CPU 100% → Pending 발생" 원인 연결. 모든 노드에 각 1개씩 뜸 |
| **PostgreSQL EC2** (온프레미스) | postgresql_exporter (EC2 직접 설치) | **9187** | Active Connection 수, 쿼리 응답시간, Lock 대기 건수 | DB 커넥션 고갈·Lock 충돌 → Pending 원인 설명 |
| **PostgreSQL / RDS** (EKS) | postgresql_exporter (k8s **Pod**) | NodePort | 동일 | RDS는 VPC 내부에서만 접근 가능 → exporter를 k8s Pod로 배포해서 NodePort로 노출 |
| **Redis EC2** (양쪽) | redis_exporter (EC2 직접 설치) | **9121** | 캐시 히트율, 메모리 사용량 | 양쪽 캐시 조건이 동일했음을 증명 — 실험 공정성 검증 |
| **AWS ALB** | 수집 안 함 | — | — | Gateway prom-client Error Rate로 충분. CloudWatch는 선택 사항 |

### 온프레미스 vs EKS 수집 방식 차이

| 컴포넌트 | 배포 방식 | 온프레미스 scrape | EKS scrape |
|---|---|---|---|
| 앱 서비스 메트릭 | k8s Pod | c8i.2xlarge **사설IP**:NodePort | Worker Node **공인IP**:NodePort |
| kube-state-metrics | k8s Deployment (1개) | c8i.2xlarge 사설IP:NodePort | Worker Node 공인IP:NodePort |
| node_exporter | DaemonSet (노드마다) | VM2 `192.168.122.3`:**9100** / VM3 `192.168.122.4`:**9100** | Worker Node1 공인IP:**9100** / Node2 공인IP:**9100** |
| PostgreSQL Exporter | EC2 직접 (온프레미스) / k8s Pod (EKS) | PostgreSQL EC2 사설IP:**9187** | k8s Pod → Worker Node 공인IP:NodePort |
| Redis Exporter | EC2 직접 설치 | Redis EC2 사설IP:**9121** | Redis EC2 **공인IP**:9121 |

*온프레미스 k8s vs AWS EKS 비교 실험 | 모니터링 구성 가이드*
